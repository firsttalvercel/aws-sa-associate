import type { Question } from '../../lib/types'

export const sqs_sns_eventbridge_questions: Question[] = [
  { id: 'd2-sqs-001', domain: 2, topics: ['SQS', 'standard queue', 'FIFO'], type: 'single', stem: 'An application processes financial transactions and requires that each transaction is processed exactly once and in the exact order it was submitted. Which SQS queue type should be used?', options: [{ id: 'A', text: 'SQS Standard queue — highest throughput' }, { id: 'B', text: 'SQS FIFO queue — exactly-once processing and strict message ordering' }, { id: 'C', text: 'SQS Standard with message deduplication' }, { id: 'D', text: 'SNS FIFO topic' }], correct: ['B'], explanation: 'SQS FIFO queues guarantee exactly-once processing (deduplication) and strict first-in, first-out ordering within message groups. Essential for financial transactions where ordering and deduplication matter.', distractors: { A: 'Standard queues offer best-effort ordering and at-least-once delivery — messages may arrive out of order or be duplicated.', C: 'Standard queues don\'t support deduplication — only FIFO queues have this built-in.', D: 'SNS FIFO topics are for fan-out — they still need an SQS FIFO queue for consuming.' } },
  { id: 'd2-sqs-002', domain: 2, topics: ['SQS', 'visibility timeout', 'retry'], type: 'single', stem: 'A Lambda function processes SQS messages. Processing occasionally takes longer than expected and messages are being processed multiple times. What should be adjusted?', options: [{ id: 'A', text: 'Increase the SQS queue retention period' }, { id: 'B', text: 'Increase the visibility timeout to exceed the maximum Lambda processing time' }, { id: 'C', text: 'Enable FIFO queue' }, { id: 'D', text: 'Add a Dead Letter Queue (DLQ)' }], correct: ['B'], explanation: 'Visibility timeout controls how long a message is hidden from other consumers after being received. If processing takes longer than the visibility timeout, the message becomes visible again and is processed again. Set the timeout to exceed maximum expected processing time.', distractors: { A: 'Retention period controls how long messages stay in the queue before being deleted — not related to duplicate processing.', C: 'FIFO ordering doesn\'t prevent duplicate processing from timeout expiry.', D: 'A DLQ captures failed messages after max retries — doesn\'t prevent timeout-based redelivery.' } },
  { id: 'd2-sqs-003', domain: 2, topics: ['SQS', 'dead letter queue', 'DLQ'], type: 'single', stem: 'Messages in an SQS queue are repeatedly failing to process. After 5 failed attempts, the messages should be moved to a separate queue for analysis. How is this configured?', options: [{ id: 'A', text: 'Configure a DLQ with maxReceiveCount set to 5 on the source queue' }, { id: 'B', text: 'Use an EventBridge rule to move failed messages' }, { id: 'C', text: 'Set up a CloudWatch alarm to trigger a Lambda to move messages' }, { id: 'D', text: 'SQS automatically moves messages after failure — no configuration needed' }], correct: ['A'], explanation: 'Configure a Dead Letter Queue (DLQ) on the source queue. Set maxReceiveCount to 5 — after a message has been received 5 times without being deleted (i.e., failed 5 times), SQS moves it to the DLQ automatically.', distractors: { B: 'EventBridge doesn\'t have visibility into individual SQS message failures.', C: 'This would require custom polling logic — the native DLQ mechanism is simpler and built-in.', D: 'SQS requires explicit DLQ configuration — it doesn\'t automatically handle failures.' } },
  { id: 'd2-sqs-004', domain: 2, topics: ['SNS', 'fan-out', 'SQS'], type: 'single', stem: 'An order processing system needs to notify an inventory service, a shipping service, and a billing service when a new order arrives. Each service processes independently. What architecture is recommended?', options: [{ id: 'A', text: 'Use SQS and have all services poll the same queue' }, { id: 'B', text: 'SNS topic with SQS queues subscribed — SNS fans out one message to all subscribed queues' }, { id: 'C', text: 'Three separate SQS queues with an application copying messages to each' }, { id: 'D', text: 'EventBridge with three separate rules' }], correct: ['B'], explanation: 'The SNS fan-out pattern uses one SNS topic with multiple SQS queues subscribed. When a message is published to SNS, it\'s delivered to all subscribed queues simultaneously. Each service independently consumes from its own queue.', distractors: { A: 'Multiple services on one queue means only one service processes each message — other services miss it.', C: 'Application-side copying creates tight coupling and a single point of failure.', D: 'EventBridge works but SNS+SQS is the canonical fan-out pattern for simple pub/sub.' } },
  { id: 'd2-sqs-005', domain: 2, topics: ['SQS', 'long polling', 'short polling'], type: 'single', stem: 'An application polls an SQS queue frequently and is incurring high costs from many empty responses. How should polling be optimized?', options: [{ id: 'A', text: 'Increase polling frequency' }, { id: 'B', text: 'Use SQS long polling — WaitTimeSeconds up to 20 seconds eliminates empty responses' }, { id: 'C', text: 'Switch to SQS FIFO queue' }, { id: 'D', text: 'Use short polling with smaller batch sizes' }], correct: ['B'], explanation: 'Long polling (WaitTimeSeconds 1-20) keeps the connection open until messages arrive or the timeout expires. This eliminates empty responses, reduces cost, and decreases message latency vs. frequent short-poll requests.', distractors: { A: 'Higher frequency means more empty responses and higher costs.', C: 'Queue type doesn\'t affect polling behavior or cost from empty responses.', D: 'Short polling returns immediately — smaller batches just mean even more empty calls.' } },
  { id: 'd2-sqs-006', domain: 2, topics: ['SQS', 'message groups', 'FIFO'], type: 'single', stem: 'An e-commerce platform uses SQS FIFO to process orders. Orders for different customers should be processed in parallel, but orders for the same customer must be processed in sequence. How should message groups be configured?', options: [{ id: 'A', text: 'Use a single MessageGroupId for all messages' }, { id: 'B', text: 'Use the customer ID as the MessageGroupId — parallel processing across groups, sequential within each group' }, { id: 'C', text: 'Use a random MessageGroupId for each message' }, { id: 'D', text: 'FIFO queues don\'t support parallel processing' }], correct: ['B'], explanation: 'SQS FIFO message groups use MessageGroupId to partition messages. Within a group, messages are processed in FIFO order. Different groups can be processed in parallel by different consumers, enabling parallelism while maintaining per-customer ordering.', distractors: { A: 'A single group ID means sequential processing of all messages — no parallelism.', C: 'Random group IDs lose ordering guarantees entirely — each message is its own group.', D: 'FIFO queues do support parallel processing via multiple message groups.' } },
  { id: 'd2-sqs-007', domain: 2, topics: ['SQS', 'delay queue', 'message timer'], type: 'single', stem: 'A payment system needs to delay order confirmation emails by 60 seconds after order creation to allow for fraud checking. Which SQS feature enables this?', options: [{ id: 'A', text: 'SQS visibility timeout' }, { id: 'B', text: 'SQS Delay Queue (default delivery delay) or per-message delay timers' }, { id: 'C', text: 'SQS message retention period' }, { id: 'D', text: 'SQS long polling' }], correct: ['B'], explanation: 'SQS delay queues postpone delivery of new messages. Set a delivery delay of 0-900 seconds (15 min) at the queue level or per-message. Messages won\'t be visible to consumers until the delay expires.', distractors: { A: 'Visibility timeout hides a message after it\'s been received — not before the first delivery.', C: 'Retention period controls how long messages stay in the queue — not when they\'re first delivered.', D: 'Long polling optimizes how consumers receive messages — not message delivery timing.' } },
  { id: 'd2-sqs-008', domain: 2, topics: ['SNS', 'message filtering'], type: 'single', stem: 'An SNS topic receives events from multiple sources. A subscription for the billing service should only receive "PAYMENT" event types, not "SHIPMENT" events. How is this configured?', options: [{ id: 'A', text: 'Have the billing service filter messages itself after receiving them' }, { id: 'B', text: 'Set a subscription filter policy on the SNS subscription — only messages matching the filter are delivered' }, { id: 'C', text: 'Create a separate SNS topic for payment events' }, { id: 'D', text: 'SNS delivers all messages to all subscribers — filtering is not possible' }], correct: ['B'], explanation: 'SNS subscription filter policies allow subscribers to receive only a subset of messages. The filter is based on message attributes. Messages that don\'t match the filter are not delivered to that subscriber.', distractors: { A: 'Consumer-side filtering wastes bandwidth and processing — SNS filter policies prevent delivery entirely.', C: 'Separate topics is more complex and creates more management overhead.', D: 'SNS subscription filter policies are a built-in feature for selective delivery.' } },
  { id: 'd2-sqs-009', domain: 2, topics: ['EventBridge', 'event bus', 'rules'], type: 'single', stem: 'A company wants to respond to EC2 instance state changes by triggering a Lambda function. What is the simplest AWS-native approach?', options: [{ id: 'A', text: 'Poll the EC2 API for state changes every minute' }, { id: 'B', text: 'Amazon EventBridge — create a rule that matches EC2 state change events and targets a Lambda function' }, { id: 'C', text: 'Subscribe to EC2 SNS notifications' }, { id: 'D', text: 'Use CloudTrail to detect API calls and trigger Lambda' }], correct: ['B'], explanation: 'EventBridge (formerly CloudWatch Events) automatically receives events from AWS services like EC2. Create a rule matching the "EC2 Instance State-change Notification" event pattern and target a Lambda function.', distractors: { A: 'Polling is inefficient — EventBridge pushes events as they happen.', C: 'EC2 doesn\'t send state changes to SNS by default — EventBridge is the native integration.', D: 'CloudTrail logs API calls (RunInstances, TerminateInstances) — not real-time state change events. EventBridge is more direct.' } },
  { id: 'd2-sqs-010', domain: 2, topics: ['EventBridge', 'scheduled rules', 'cron'], type: 'single', stem: 'A data processing job needs to run every day at 2:00 AM UTC. What is the simplest AWS-native solution?', options: [{ id: 'A', text: 'EC2 cron job' }, { id: 'B', text: 'EventBridge scheduled rule with a cron expression, targeting a Lambda function' }, { id: 'C', text: 'CloudWatch alarm on a time metric' }, { id: 'D', text: 'Step Functions wait state' }], correct: ['B'], explanation: 'EventBridge scheduled rules support cron expressions (cron(0 2 * * ? *)) or rate expressions. They trigger targets (Lambda, ECS tasks, Step Functions) at the scheduled time. No infrastructure to maintain.', distractors: { A: 'EC2 cron requires a running instance 24/7 — inefficient and more expensive.', C: 'CloudWatch alarms are threshold-based — not time-based schedulers.', D: 'Step Functions wait states pause within a running execution — not for initial scheduling.' } },
  { id: 'd2-sqs-011', domain: 2, topics: ['SQS', 'encryption', 'SSE'], type: 'single', stem: 'An SQS queue contains sensitive customer data. The data must be encrypted at rest. How is this configured?', options: [{ id: 'A', text: 'Encrypt the messages in the application before sending' }, { id: 'B', text: 'Enable SQS Server-Side Encryption (SSE) using KMS — messages encrypted at rest automatically' }, { id: 'C', text: 'Use HTTPS for SQS API calls' }, { id: 'D', text: 'SQS encrypts all messages by default' }], correct: ['B'], explanation: 'SQS SSE uses AWS KMS to encrypt message contents at rest. Can use an AWS managed key (free tier) or a customer-managed key (CMK) for additional control and auditability.', distractors: { A: 'Application-level encryption works but adds complexity and requires key management — SSE is simpler.', C: 'HTTPS encrypts data in transit — not at rest.', D: 'SQS does not encrypt by default — SSE must be explicitly enabled.' } },
  { id: 'd2-sqs-012', domain: 2, topics: ['SNS', 'FIFO topic', 'ordering'], type: 'single', stem: 'A stock trading platform uses SNS to publish trade events. Downstream systems must receive events in the exact order they were published. What SNS feature is required?', options: [{ id: 'A', text: 'SNS Standard topic with timestamps' }, { id: 'B', text: 'SNS FIFO topic — strict ordering and message deduplication across subscribers' }, { id: 'C', text: 'Add sequence numbers to message attributes' }, { id: 'D', text: 'Standard SNS guarantees ordering by default' }], correct: ['B'], explanation: 'SNS FIFO topics guarantee strict message ordering and deduplication, similar to SQS FIFO. Use with SQS FIFO subscriptions for end-to-end ordering guarantees. Required for financial or sequencing-critical applications.', distractors: { A: 'Standard SNS topics don\'t guarantee ordering — timestamps help but don\'t enforce delivery order.', C: 'Sequence numbers in attributes require consumers to implement their own ordering logic.', D: 'Standard SNS is at-least-once, best-effort ordering — not guaranteed.' } },
  { id: 'd2-sqs-013', domain: 2, topics: ['EventBridge', 'event archive', 'replay'], type: 'single', stem: 'A company wants to replay historical events through EventBridge to reprocess data after deploying a bug fix. What feature enables this?', options: [{ id: 'A', text: 'CloudTrail event history' }, { id: 'B', text: 'EventBridge event archive and replay — archive events, then replay them through rules' }, { id: 'C', text: 'SQS message retention' }, { id: 'D', text: 'CloudWatch Logs Insights' }], correct: ['B'], explanation: 'EventBridge event archiving stores events indefinitely (or for a configured period). Event replay replays archived events through the same or different rules, allowing reprocessing after code fixes.', distractors: { A: 'CloudTrail logs API calls — not application events for replay.', C: 'SQS retention holds unconsumed messages — not events for replay after consumption.', D: 'CloudWatch Logs Insights queries logs — not event replay.' } },
  { id: 'd2-sqs-014', domain: 2, topics: ['SQS', 'Lambda trigger', 'batch size'], type: 'single', stem: 'Lambda is triggered by an SQS queue and processes messages in batches. Some messages in a batch fail processing. How should the Lambda be configured to avoid reprocessing the successful messages?', options: [{ id: 'A', text: 'Lambda should delete each message immediately after processing it' }, { id: 'B', text: 'Use SQS batch item failure reporting — Lambda returns only the failed message IDs so SQS retries only those' }, { id: 'C', text: 'Reduce batch size to 1 so only one message fails at a time' }, { id: 'D', text: 'Use a FIFO queue for partial batch handling' }], correct: ['B'], explanation: 'Lambda supports partial batch response for SQS. The Lambda returns a response containing only the IDs of failed messages. SQS treats successful messages as processed (deleted) and only retries the failed ones.', distractors: { A: 'Lambda doesn\'t have permission to delete SQS messages — SQS deletes them when Lambda returns success.', C: 'Batch size 1 works but loses the throughput benefits of batching.', D: 'FIFO vs Standard queue type doesn\'t change how partial batch failures are handled.' } },
  { id: 'd2-sqs-015', domain: 2, topics: ['SNS', 'SMS', 'mobile push'], type: 'single', stem: 'An application needs to send SMS notifications to users and push notifications to iOS/Android apps. Which AWS service provides this capability?', options: [{ id: 'A', text: 'SES (Simple Email Service)' }, { id: 'B', text: 'Amazon SNS — supports SMS, mobile push (APNs, FCM/GCM), and email endpoints' }, { id: 'C', text: 'Pinpoint only' }, { id: 'D', text: 'SQS with a mobile SDK' }], correct: ['B'], explanation: 'SNS supports multiple endpoint types including SMS, mobile push notifications (Apple APNs, Google FCM), email, and HTTP/HTTPS. It\'s the native AWS service for multi-channel notifications.', distractors: { A: 'SES handles email only — not SMS or mobile push.', C: 'Pinpoint is for customer engagement campaigns — SNS is the simpler, lower-level notification primitive.', D: 'SQS is a message queue, not a notification service.' } },
  { id: 'd2-sqs-016', domain: 2, topics: ['EventBridge', 'custom event bus', 'SaaS'], type: 'single', stem: 'A company wants to route events from Zendesk (a SaaS application) directly into their AWS event processing pipeline without custom polling code. What should they use?', options: [{ id: 'A', text: 'AWS Lambda polling the Zendesk API' }, { id: 'B', text: 'EventBridge partner event bus — Zendesk integrates natively as an event source' }, { id: 'C', text: 'API Gateway receiving Zendesk webhooks' }, { id: 'D', text: 'SNS with Zendesk HTTPS subscription' }], correct: ['B'], explanation: 'EventBridge supports SaaS partner event sources (Zendesk, Datadog, Salesforce, etc.). Events from these partners flow directly into partner event buses without custom polling or Lambda code.', distractors: { A: 'Polling requires maintaining Lambda code and Zendesk API credentials — EventBridge partnership is simpler.', C: 'API Gateway + Lambda works but requires more custom code vs. native EventBridge partner integration.', D: 'SNS HTTPS subscriptions receive webhooks but lack the EventBridge schema registry and routing features.' } },
  { id: 'd2-sqs-017', domain: 2, topics: ['SQS', 'access policy', 'cross-account'], type: 'single', stem: 'An SQS queue in Account A needs to accept messages from an SNS topic in Account B. What is required?', options: [{ id: 'A', text: 'VPC peering between the two accounts' }, { id: 'B', text: 'Add an SQS access policy on the queue allowing sns:SendMessage from the Account B SNS topic ARN' }, { id: 'C', text: 'Use SQS FIFO for cross-account messaging' }, { id: 'D', text: 'Cross-account SQS messaging is not supported' }], correct: ['B'], explanation: 'SQS access policies (resource-based policies) allow cross-account access. Grant the Account B SNS topic ARN permission to sqs:SendMessage on the queue. SNS needs this permission to deliver messages cross-account.', distractors: { A: 'VPC peering is for network connectivity — SQS and SNS are public AWS services, no VPC peering needed.', C: 'Queue type doesn\'t determine cross-account access — the access policy controls this.', D: 'Cross-account SQS access is fully supported with the right resource policy.' } },
  { id: 'd2-sqs-018', domain: 2, topics: ['EventBridge', 'pipes', 'integration'], type: 'single', stem: 'A team wants to connect an SQS queue to a Lambda function with built-in filtering and transformation, without writing custom polling code. What EventBridge feature enables this?', options: [{ id: 'A', text: 'EventBridge Rules' }, { id: 'B', text: 'EventBridge Pipes — point-to-point connection with filtering and transformation between source and target' }, { id: 'C', text: 'Step Functions with SQS activity' }, { id: 'D', text: 'Lambda event source mapping' }], correct: ['B'], explanation: 'EventBridge Pipes provide a point-to-point integration between a source (SQS, DynamoDB Streams, Kinesis, etc.) and a target (Lambda, Step Functions, etc.) with optional filtering and enrichment steps built in.', distractors: { A: 'EventBridge Rules route events from event buses — not direct queue-to-target connections with enrichment.', C: 'Step Functions with SQS activity works but requires more setup.', D: 'Lambda event source mapping is simpler but lacks the enrichment/filtering capabilities of EventBridge Pipes.' } },
  { id: 'd2-sqs-019', domain: 2, topics: ['SNS', 'message durability'], type: 'single', stem: 'An SNS topic delivers messages to an HTTP endpoint. If the endpoint is temporarily unavailable, how does SNS handle delivery?', options: [{ id: 'A', text: 'The message is dropped' }, { id: 'B', text: 'SNS retries with exponential backoff — up to 100,015 total retries over 23 days for HTTP endpoints' }, { id: 'C', text: 'SNS waits indefinitely until the endpoint recovers' }, { id: 'D', text: 'SNS redirects to a backup endpoint' }], correct: ['B'], explanation: 'SNS retries HTTP/HTTPS delivery with an exponential backoff policy. The retry schedule has phases: immediate, pre-backoff, backoff, and post-backoff. After all retries are exhausted, the message is discarded (unless a DLQ is configured).', distractors: { A: 'SNS doesn\'t immediately drop messages — it retries extensively before giving up.', C: 'SNS has a finite retry window — after 23 days of maximum retries, the message is dropped.', D: 'SNS doesn\'t automatically failover to backup endpoints — configure a DLQ to capture undelivered messages.' } },
  { id: 'd2-sqs-020', domain: 2, topics: ['SQS', 'throughput', 'FIFO limits'], type: 'single', stem: 'An SQS FIFO queue is hitting the 3,000 messages/second throughput limit. How can throughput be increased?', options: [{ id: 'A', text: 'Switch to Standard queue' }, { id: 'B', text: 'Enable FIFO high throughput mode — increases to 30,000 messages/second (with batching up to 90,000)' }, { id: 'C', text: 'Add more producers' }, { id: 'D', text: 'Increase message batch size' }], correct: ['B'], explanation: 'SQS FIFO high throughput mode increases the quota from 3,000 to 30,000 API calls per second per API action. With batching (max 10 messages per batch), effective throughput reaches 90,000 messages per second.', distractors: { A: 'Switching to Standard loses ordering and exactly-once guarantees — not appropriate if those are required.', C: 'More producers don\'t increase the queue throughput limit.', D: 'Larger batches help with efficiency but without high throughput mode, the API rate limit still applies.' } },
  { id: 'd2-sqs-021', domain: 2, topics: ['EventBridge', 'schema registry'], type: 'single', stem: 'A development team wants to discover event schemas published to EventBridge and auto-generate code bindings for their application. What feature helps them?', options: [{ id: 'A', text: 'CloudTrail event history' }, { id: 'B', text: 'EventBridge Schema Registry — discovers and stores event schemas, generates code bindings' }, { id: 'C', text: 'API Gateway model schemas' }, { id: 'D', text: 'JSON Schema Validator' }], correct: ['B'], explanation: 'EventBridge Schema Registry automatically discovers event schemas and stores them. It can generate code bindings (Java, Python, TypeScript) from schemas, making it easier for developers to work with events in a type-safe way.', distractors: { A: 'CloudTrail logs API calls — not event schema discovery.', C: 'API Gateway models define REST API request/response schemas — not EventBridge event schemas.', D: 'JSON Schema Validator is a tool — not an integrated AWS discovery feature.' } },
  { id: 'd2-sqs-022', domain: 2, topics: ['SQS', 'message retention'], type: 'single', stem: 'Messages in an SQS queue are not consumed for several days due to a consumer outage. The default retention period expires some messages. What retention period configuration maximizes message preservation?', options: [{ id: 'A', text: 'SQS default retention is 4 days' }, { id: 'B', text: 'Maximum SQS message retention is 14 days — configure the queue for maximum retention' }, { id: 'C', text: 'SQS retains messages indefinitely' }, { id: 'D', text: 'Maximum SQS retention is 7 days' }], correct: ['B'], explanation: 'SQS message retention can be configured from 1 minute to 14 days. The default is 4 days. For long outage tolerance, set retention to 14 days. Messages older than the retention period are automatically deleted.', distractors: { A: '4 days is the default, not the maximum. The maximum is 14 days.', C: 'SQS does not retain messages indefinitely — they are deleted after the retention period.', D: '7 days is not the maximum — SQS supports up to 14 days.' } },
  { id: 'd2-sqs-023', domain: 2, topics: ['SNS', 'raw message delivery'], type: 'single', stem: 'An SQS queue subscribed to an SNS topic is receiving messages wrapped in SNS JSON metadata. The consuming application expects raw message content. How can this be fixed?', options: [{ id: 'A', text: 'Change the queue type to FIFO' }, { id: 'B', text: 'Enable raw message delivery on the SNS subscription — SQS receives the raw message body without SNS envelope' }, { id: 'C', text: 'Configure an SQS message transform' }, { id: 'D', text: 'Use EventBridge instead of SNS' }], correct: ['B'], explanation: 'SNS subscription raw message delivery strips the SNS JSON envelope (Message, MessageId, Subject, etc.) from the message. The SQS queue receives only the original message body, which simplifies consumer code.', distractors: { A: 'Queue type doesn\'t affect SNS message envelope delivery.', C: 'SQS doesn\'t have a native message transform feature.', D: 'Switching to EventBridge is a significant architecture change — raw message delivery solves this with a checkbox.' } },
  { id: 'd2-sqs-024', domain: 2, topics: ['EventBridge', 'targets', 'dead letter'], type: 'single', stem: 'An EventBridge rule targets a Lambda function. If Lambda is throttled or unavailable, EventBridge retries delivery for up to 24 hours and then discards the event. How can undelivered events be preserved?', options: [{ id: 'A', text: 'Increase Lambda concurrency limit' }, { id: 'B', text: 'Configure a dead-letter queue (SQS) or dead-letter topic (SNS) on the EventBridge rule target' }, { id: 'C', text: 'Set EventBridge retry policy to unlimited' }, { id: 'D', text: 'Enable EventBridge archive' }], correct: ['B'], explanation: 'EventBridge target configuration supports a dead-letter queue or SNS topic. After all retries are exhausted, failed events are sent to the DLQ/DLT for analysis and manual reprocessing.', distractors: { A: 'More Lambda concurrency helps prevent throttling but doesn\'t capture events that fail during outages.', C: 'EventBridge retry policy is limited to 185 retries over 24 hours — unlimited is not an option.', D: 'Archive captures events at the event bus level — not specifically failed delivery attempts to a target.' } },
  { id: 'd2-sqs-025', domain: 2, topics: ['SQS', 'exactly once', 'deduplication'], type: 'single', stem: 'An SQS FIFO queue is receiving duplicate messages from producers. How are duplicates prevented within a 5-minute window?', options: [{ id: 'A', text: 'Enable long polling on the queue' }, { id: 'B', text: 'Set a MessageDeduplicationId — FIFO queue deduplicates messages with the same ID within 5 minutes' }, { id: 'C', text: 'Use a Standard queue with manual deduplication logic' }, { id: 'D', text: 'Enable content-based deduplication — FIFO queue generates a hash of the message body as the deduplication ID' }], correct: ['D'], explanation: 'For FIFO queues, enable content-based deduplication. The queue generates a SHA-256 hash of the message body as the MessageDeduplicationId. Duplicate messages with the same body within 5 minutes are rejected. Or producers can provide explicit MessageDeduplicationIds.', distractors: { A: 'Long polling affects how consumers receive messages — not deduplication.', B: 'Explicit MessageDeduplicationId works but content-based deduplication handles this automatically without producer changes.', C: 'Standard queues don\'t support deduplication — and manual logic adds complexity.' } },
  { id: 'd2-sqs-026', domain: 2, topics: ['SNS', 'delivery status logging'], type: 'single', stem: 'A team needs to verify that SNS is successfully delivering messages to all HTTP endpoints. How can delivery status be monitored?', options: [{ id: 'A', text: 'Check CloudTrail for SNS:Publish calls' }, { id: 'B', text: 'Enable SNS delivery status logging — writes success/failure logs to CloudWatch Logs per subscription' }, { id: 'C', text: 'Monitor the receiving endpoint\'s access logs' }, { id: 'D', text: 'Use X-Ray to trace SNS deliveries' }], correct: ['B'], explanation: 'SNS delivery status logging (configured per subscription type — HTTP, Lambda, SQS, etc.) writes delivery attempt outcomes to CloudWatch Logs, including success/failure, HTTP status codes, and response body (for HTTP endpoints).', distractors: { A: 'CloudTrail logs the Publish API call — not whether the downstream delivery succeeded.', C: 'Endpoint access logs capture received requests but don\'t show SNS delivery failures (the endpoint never receives a failed delivery).', D: 'X-Ray supports Lambda/API Gateway traces — not native SNS delivery status.' } },
  { id: 'd2-sqs-027', domain: 2, topics: ['EventBridge', 'multi-account', 'organization'], type: 'single', stem: 'A company wants to centralize events from all AWS accounts in their organization into a single account for monitoring. What EventBridge feature supports this?', options: [{ id: 'A', text: 'One EventBridge rule per account routing to the central account' }, { id: 'B', text: 'EventBridge cross-account event bus policy — allow the organization to put events on the central event bus' }, { id: 'C', text: 'CloudTrail organization trail' }, { id: 'D', text: 'AWS Config aggregator' }], correct: ['B'], explanation: 'EventBridge event buses support resource policies. Grant the entire AWS organization permission to publish events to the central account\'s event bus using aws:PrincipalOrgID condition. All accounts can then forward events centrally.', distractors: { A: 'Per-account rules would work but require individual configuration per account — the organization-wide policy is simpler.', C: 'CloudTrail centralization captures API calls — not custom application events.', D: 'Config aggregator collects compliance findings — not application events.' } },
  { id: 'd2-sqs-028', domain: 2, topics: ['SQS', 'message size', 'S3'], type: 'single', stem: 'An application needs to send 512 KB messages through SQS. The maximum SQS message size is 256 KB. What pattern handles large messages?', options: [{ id: 'A', text: 'Split messages into multiple SQS messages' }, { id: 'B', text: 'Use the SQS extended client library — store the payload in S3 and send only the S3 reference in SQS' }, { id: 'C', text: 'Switch to SNS which supports larger messages' }, { id: 'D', text: 'Compress the message to under 256 KB' }], correct: ['B'], explanation: 'The Amazon SQS Extended Client Library (available for Java, Python, etc.) automatically stores messages larger than the threshold in S3 and puts the S3 pointer in the SQS message. Consumers retrieve the payload from S3 transparently.', distractors: { A: 'Splitting messages creates ordering and reassembly complexity.', C: 'SNS also has a 256 KB message size limit — same constraint.', D: 'Compression may work for some messages but is not guaranteed and adds complexity.' } },
  { id: 'd2-sqs-029', domain: 2, topics: ['EventBridge', 'input transformer'], type: 'single', stem: 'An EventBridge rule receives EC2 state change events and must invoke a Lambda function with only the instance ID and state — not the full event. What feature transforms the event?', options: [{ id: 'A', text: 'Lambda code that parses the full event' }, { id: 'B', text: 'EventBridge Input Transformer — extracts specific fields from the event and formats them for the target' }, { id: 'C', text: 'EventBridge Content Filtering' }, { id: 'D', text: 'SQS message transform' }], correct: ['B'], explanation: 'EventBridge Input Transformers use JSONPath to extract fields from event JSON and create a custom input template. Lambda receives the transformed, minimal payload instead of the full event.', distractors: { A: 'Lambda parsing works but every Lambda invocation receives the full event payload — wasteful and adds code complexity.', C: 'Content filtering determines which events trigger the rule — not how the event is shaped for the target.', D: 'SQS doesn\'t have a message transform capability.' } },
  { id: 'd2-sqs-030', domain: 2, topics: ['SQS', 'approximate messages', 'scaling'], type: 'single', stem: 'An Auto Scaling group needs to scale EC2 instances based on the number of messages waiting in an SQS queue. What CloudWatch metric drives this scaling?', options: [{ id: 'A', text: 'SQS:NumberOfMessagesSent' }, { id: 'B', text: 'SQS:ApproximateNumberOfMessagesVisible — number of messages available in the queue' }, { id: 'C', text: 'SQS:NumberOfMessagesDeleted' }, { id: 'D', text: 'EC2:CPUUtilization' }], correct: ['B'], explanation: 'ApproximateNumberOfMessagesVisible is the key metric for SQS-based scaling. As this value increases, scale out. A common pattern: target tracking on ApproximateNumberOfMessagesVisible / number of instances = messages-per-instance.', distractors: { A: 'NumberOfMessagesSent tracks messages entering the queue — not the backlog depth.', C: 'NumberOfMessagesDeleted tracks processed messages — not the current backlog.', D: 'CPU utilization scales based on processor load, not queue depth.' } },
  { id: 'd2-sqs-031', domain: 2, topics: ['SNS', 'email subscription', 'confirmation'], type: 'single', stem: 'An SNS topic has an email subscription pending confirmation. Messages are published but the email address is not receiving them. Why?', options: [{ id: 'A', text: 'Email is not a supported SNS endpoint type' }, { id: 'B', text: 'Email subscriptions require explicit confirmation — the subscriber must click the confirmation link before receiving messages' }, { id: 'C', text: 'SNS has a delay of up to 24 hours for email delivery' }, { id: 'D', text: 'The SNS topic must be in the same region as the email server' }], correct: ['B'], explanation: 'SNS email subscriptions are pending until the subscriber confirms via the confirmation link sent to the email address. Messages published before confirmation (or without confirmation) are not delivered to that endpoint.', distractors: { A: 'Email is a supported SNS subscription type.', C: 'SNS email delivery is typically immediate once confirmed — there\'s no intentional 24-hour delay.', D: 'SNS email delivery is independent of email server region.' } },
  { id: 'd2-sqs-032', domain: 2, topics: ['EventBridge', 'API destination', 'third-party'], type: 'single', stem: 'A company wants EventBridge to call an external REST API (a third-party webhook) when specific events occur. What feature supports this?', options: [{ id: 'A', text: 'EventBridge cannot call external APIs' }, { id: 'B', text: 'EventBridge API Destinations — configure an HTTP endpoint with authentication for EventBridge to call' }, { id: 'C', text: 'Use Lambda as an intermediary to call the external API' }, { id: 'D', text: 'AWS AppSync for external API calls' }], correct: ['B'], explanation: 'EventBridge API Destinations allow EventBridge rules to deliver events to any external HTTP/HTTPS endpoint. Configure the URL, HTTP method, and a Connection (which handles authentication: API key, OAuth, basic auth).', distractors: { A: 'EventBridge API Destinations explicitly provide this capability.', C: 'Lambda works but API Destinations avoids the need to write and maintain Lambda code.', D: 'AppSync is GraphQL API infrastructure — not an EventBridge integration for outbound webhooks.' } },
  { id: 'd2-sqs-033', domain: 2, topics: ['SQS', 'queue depth', 'backpressure'], type: 'single', stem: 'A microservices system uses SQS to decouple services. The consuming service is slower than the producer. What pattern ensures the producer does not overwhelm the system?', options: [{ id: 'A', text: 'Increase SQS queue size' }, { id: 'B', text: 'The SQS queue naturally buffers messages — the producer continues writing; consumers process at their own pace' }, { id: 'C', text: 'Configure SQS to block producer sends when the queue is full' }, { id: 'D', text: 'Use SNS with SQS to balance load' }], correct: ['B'], explanation: 'SQS acts as a buffer (queue) between producers and consumers. The queue absorbs the difference in processing rates. Messages wait until consumers are ready. This is the core value of message queuing for decoupling.', distractors: { A: 'SQS doesn\'t have a configurable "queue size limit" — it can hold unlimited messages (limited by retention period).', C: 'SQS doesn\'t block producers — it accepts messages as long as they are within size and quota limits.', D: 'The SQS queue itself provides the buffering without needing SNS.' } },
  { id: 'd2-sqs-034', domain: 2, topics: ['SNS', 'SQS', 'durability'], type: 'single', stem: 'A company wants to ensure that notification events are not lost if the consuming service is temporarily unavailable. How should the architecture be designed?', options: [{ id: 'A', text: 'Publish directly to the consuming service endpoint via SNS' }, { id: 'B', text: 'SNS topic → SQS queue → consumer — SQS durably stores messages while the consumer is down' }, { id: 'C', text: 'SNS retries indefinitely until delivery succeeds' }, { id: 'D', text: 'Write events to S3 and have the consumer poll S3' }], correct: ['B'], explanation: 'The SNS+SQS fan-out pattern adds durability to SNS. SNS delivers to the SQS queue (which is highly durable and persists messages for up to 14 days). The consumer can recover and process messages when it comes back online.', distractors: { A: 'HTTP endpoint delivery: SNS retries but after the retry window, messages are lost unless a DLQ is configured.', C: 'SNS HTTP/HTTPS retries have a limit — after 23 days, messages are discarded.', D: 'S3 polling is an anti-pattern for event-driven architectures — SQS is the right durable buffer.' } },
  { id: 'd2-sqs-035', domain: 2, topics: ['EventBridge', 'content filtering', 'prefix'], type: 'single', stem: 'An EventBridge rule should only match S3 events where the object key starts with "uploads/". What filter capability handles this?', options: [{ id: 'A', text: 'Create a Lambda function to filter events' }, { id: 'B', text: 'EventBridge event pattern with prefix matching on the key field: {"prefix": "uploads/"}' }, { id: 'C', text: 'EventBridge doesn\'t support prefix matching' }, { id: 'D', text: 'Use S3 event notifications with prefix filtering instead' }], correct: ['B'], explanation: 'EventBridge event patterns support content filtering including prefix, suffix, numeric ranges, anything-but, and exists/not-exists. For a key prefix match: { "detail": { "object": { "key": [{ "prefix": "uploads/" }] } } }', distractors: { A: 'Lambda for filtering wastes invocations and costs — EventBridge content filtering prevents delivery in the first place.', C: 'EventBridge prefix, suffix, and other content-based filters are well-supported.', D: 'S3 event notifications work but limit the processing pipeline to S3 events — EventBridge provides a unified event router.' } },
  { id: 'd2-sqs-036', domain: 2, topics: ['SQS', 'poison pill', 'DLQ'], type: 'single', stem: 'A message in an SQS queue causes the consumer to crash every time it processes it. The message keeps becoming visible again and crashing consumers. What prevents this from happening indefinitely?', options: [{ id: 'A', text: 'Increase the visibility timeout' }, { id: 'B', text: 'Configure a DLQ with maxReceiveCount — after N failed receives, SQS moves the poison message to the DLQ' }, { id: 'C', text: 'Delete the queue and recreate it' }, { id: 'D', text: 'Use a FIFO queue to prevent processing the bad message' }], correct: ['B'], explanation: 'A DLQ with a maxReceiveCount prevents poison pill messages from cycling indefinitely. After the message is received N times without deletion, it\'s moved to the DLQ. The consumer can then analyze it without crashing production processing.', distractors: { A: 'Longer visibility timeout extends the wait between crashes but doesn\'t stop the cycle.', C: 'Deleting the queue loses all other valid messages.', D: 'FIFO queue doesn\'t remove bad messages — it would actually block the entire queue for that message group.' } },
  { id: 'd2-sqs-037', domain: 2, topics: ['SNS', 'encryption', 'KMS'], type: 'single', stem: 'SNS messages containing sensitive health information must be encrypted at rest while stored in SNS. How is this enabled?', options: [{ id: 'A', text: 'SNS doesn\'t store messages — they\'re in-flight only' }, { id: 'B', text: 'Enable SNS Server-Side Encryption (SSE) with KMS — messages are encrypted while stored in SNS buffers' }, { id: 'C', text: 'Use HTTPS for all SNS API calls' }, { id: 'D', text: 'SNS encrypts all messages by default' }], correct: ['B'], explanation: 'SNS SSE encrypts messages using KMS while they\'re stored in SNS (prior to delivery). Use a CMK for HIPAA/compliance scenarios requiring customer-managed keys.', distractors: { A: 'SNS stores messages temporarily (buffers) before delivering — SSE protects data during that period.', C: 'HTTPS encrypts data in transit — not data at rest in SNS.', D: 'SNS SSE is not enabled by default — it must be explicitly configured.' } },
  { id: 'd2-sqs-038', domain: 2, topics: ['EventBridge', 'Scheduler'], type: 'single', stem: 'A company needs to send 10,000 scheduled messages at specific times in the future (one per customer for their renewal date). What AWS service handles millions of one-time schedules?', options: [{ id: 'A', text: 'EventBridge scheduled rules (one rule per customer)' }, { id: 'B', text: 'Amazon EventBridge Scheduler — supports millions of one-time and recurring schedules without creating individual rules' }, { id: 'C', text: 'SQS delay queues' }, { id: 'D', text: 'Lambda with a cron trigger' }], correct: ['B'], explanation: 'EventBridge Scheduler is designed for high-scale scheduling. Create millions of schedules programmatically via API. Each schedule can target any AWS service at a specific time or recurring pattern.', distractors: { A: 'Creating 10,000+ EventBridge rules for one-time events is not scalable — Scheduler handles this without individual rules.', C: 'SQS delay queues support up to 15-minute delays — not arbitrary future dates.', D: 'A single Lambda cron would need to manage all schedules itself — complex and doesn\'t scale.' } },
  { id: 'd2-sqs-039', domain: 2, topics: ['SQS', 'throughput', 'Lambda'], type: 'single', stem: 'An SQS queue receives bursts of thousands of messages. Lambda functions triggered by SQS are scaling too slowly. What setting speeds up Lambda scaling for SQS?', options: [{ id: 'A', text: 'Increase the Lambda timeout' }, { id: 'B', text: 'Increase the event source mapping batch size and enable concurrent Lambda invocations per event source' }, { id: 'C', text: 'Switch to SNS as the trigger' }, { id: 'D', text: 'Use a FIFO queue for faster processing' }], correct: ['B'], explanation: 'For SQS Lambda triggers: increase batch size (up to 10,000 for Standard, 10 for FIFO) and set a higher maximum concurrency on the event source mapping. Lambda scales at 60 additional instances per minute for SQS.', distractors: { A: 'Lambda timeout affects maximum processing duration — not scaling speed.', C: 'SNS triggers require a different architecture — doesn\'t directly speed up SQS processing.', D: 'FIFO queues have lower throughput limits — not faster for burst processing.' } },
  { id: 'd2-sqs-040', domain: 2, topics: ['SNS', 'cross-region', 'delivery'], type: 'single', stem: 'An SNS topic in us-east-1 has subscribers in multiple regions (SQS queues in eu-west-1 and ap-southeast-1). Is this supported?', options: [{ id: 'A', text: 'No — SNS can only deliver to subscribers in the same region' }, { id: 'B', text: 'Yes — SNS supports cross-region delivery to SQS queues in other regions' }, { id: 'C', text: 'Only if the queues are encrypted with the same KMS key' }, { id: 'D', text: 'Yes, but only with SQS FIFO queues' }], correct: ['B'], explanation: 'SNS supports cross-region delivery to SQS queues. The SQS queue in another region must have a resource policy allowing the SNS topic (from us-east-1) to send messages. This enables global fan-out.', distractors: { A: 'Cross-region SNS delivery is supported and commonly used for multi-region architectures.', C: 'KMS key sharing is not required for cross-region SNS to SQS delivery.', D: 'Cross-region delivery works with both Standard and FIFO queues.' } },
  { id: 'd2-sqs-041', domain: 2, topics: ['EventBridge', 'ordering', 'at-least-once'], type: 'single', stem: 'A developer is building a system using EventBridge. They must understand EventBridge delivery guarantees. Which is accurate?', options: [{ id: 'A', text: 'EventBridge guarantees exactly-once delivery with strict ordering' }, { id: 'B', text: 'EventBridge provides at-least-once delivery — events may be delivered more than once or out of order' }, { id: 'C', text: 'EventBridge guarantees at-most-once delivery' }, { id: 'D', text: 'EventBridge guarantees exactly-once with no ordering' }], correct: ['B'], explanation: 'EventBridge delivers events at-least-once and doesn\'t guarantee strict ordering. Applications should be designed to be idempotent (handling duplicate events gracefully) when using EventBridge.', distractors: { A: 'EventBridge doesn\'t provide exactly-once or strict ordering guarantees.', C: 'At-most-once would mean some events could be dropped — EventBridge retries, making it at-least-once.', D: 'Close but incomplete — at-least-once delivery is accurate, without ordering guarantees.' } },
  { id: 'd2-sqs-042', domain: 2, topics: ['SQS', 'VPC endpoint'], type: 'single', stem: 'Lambda functions in a private VPC subnet need to send messages to SQS without internet access. What is required?', options: [{ id: 'A', text: 'NAT Gateway for Lambda to reach SQS' }, { id: 'B', text: 'VPC Endpoint for SQS (interface endpoint) — private connectivity from VPC to SQS without internet' }, { id: 'C', text: 'SQS must be deployed in the same VPC' }, { id: 'D', text: 'VPC peering to the SQS region' }], correct: ['B'], explanation: 'An SQS interface endpoint (powered by AWS PrivateLink) allows VPC resources to communicate with SQS privately. Traffic stays within the AWS network — no internet gateway, NAT gateway, or public IP required.', distractors: { A: 'NAT Gateway enables internet access — VPC Endpoint provides private access without the internet.', C: 'SQS is a regional AWS service — it doesn\'t deploy into a VPC.', D: 'VPC peering is for connecting two VPCs — not for accessing AWS services.' } },
  { id: 'd2-sqs-043', domain: 2, topics: ['EventBridge', 'event pattern', 'IAM'], type: 'single', stem: 'An EventBridge rule needs to trigger a Lambda function only when an IAM user is created in any account of the AWS organization. What is the event source?', options: [{ id: 'A', text: 'AWS IAM service events' }, { id: 'B', text: 'AWS CloudTrail events — IAM CreateUser API call appears in CloudTrail and can trigger EventBridge' }, { id: 'C', text: 'AWS Config rule evaluation' }, { id: 'D', text: 'AWS Security Hub findings' }], correct: ['B'], explanation: 'IAM is a global service that logs to CloudTrail in us-east-1. CloudTrail Management Events appear in EventBridge. Create a rule matching the IAM CreateUser API call event pattern from CloudTrail source.', distractors: { A: 'There is no separate "IAM service events" stream — IAM actions are captured via CloudTrail Management Events.', C: 'AWS Config detects configuration state — EventBridge rule on CloudTrail is more real-time.', D: 'Security Hub aggregates findings — not the trigger for IAM user creation specifically.' } },
  { id: 'd2-sqs-044', domain: 2, topics: ['SQS', 'queue metrics', 'monitoring'], type: 'single', stem: 'A team wants to alert when the age of the oldest message in an SQS queue exceeds 30 minutes, indicating processing is falling behind. What metric should the CloudWatch alarm monitor?', options: [{ id: 'A', text: 'ApproximateNumberOfMessagesVisible' }, { id: 'B', text: 'ApproximateAgeOfOldestMessage — age of the oldest non-deleted message in the queue' }, { id: 'C', text: 'NumberOfMessagesSent' }, { id: 'D', text: 'SentMessageSize' }], correct: ['B'], explanation: 'ApproximateAgeOfOldestMessage (in seconds) indicates how long the oldest message has been waiting. A high value indicates consumers are falling behind. Create a CloudWatch alarm threshold at 1800 seconds (30 minutes).', distractors: { A: 'ApproximateNumberOfMessagesVisible shows queue depth (count) — not age of messages.', C: 'NumberOfMessagesSent tracks message production rate — not processing delay.', D: 'SentMessageSize tracks message size — not age.' } },
  { id: 'd2-sqs-045', domain: 2, topics: ['SNS', 'SQS', 'fan-out', 'resilience'], type: 'multi', stem: 'A company uses SNS to fan out order events to multiple downstream services via SQS queues. Which TWO design choices improve resilience of this architecture?', options: [{ id: 'A', text: 'Enable SQS dead-letter queues for each subscribed queue' }, { id: 'B', text: 'Use the same SQS queue for all downstream services' }, { id: 'C', text: 'Enable SNS delivery status logging to CloudWatch' }, { id: 'D', text: 'Configure SQS encryption only on one queue' }], correct: ['A', 'C'], explanation: 'DLQs (A) capture messages that fail processing repeatedly, preventing data loss. SNS delivery status logging (C) monitors whether SNS is successfully delivering to all SQS endpoints.', distractors: { B: 'A single shared queue means all services share one consumer lane — if one service fails, it blocks others. Separate queues provide isolation.', D: 'Selective encryption introduces inconsistent data protection — encrypt all queues containing sensitive data.' } },
]
