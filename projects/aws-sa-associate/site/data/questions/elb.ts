import type { Question } from '../../lib/types'

export const elb_questions: Question[] = [
  { id: 'd2-elb-001', domain: 2, topics: ['ELB', 'ALB', 'routing'], type: 'single', stem: 'An application has multiple microservices at different URL paths (/api/users, /api/orders). A single load balancer should route to different target groups based on the path. Which load balancer type supports this?', options: [{ id: 'A', text: 'Network Load Balancer' }, { id: 'B', text: 'Application Load Balancer — supports path-based routing' }, { id: 'C', text: 'Classic Load Balancer' }, { id: 'D', text: 'Gateway Load Balancer' }], correct: ['B'], explanation: 'ALB supports content-based routing using listener rules. Rules can route based on URL path, hostname, HTTP headers, query strings, and source IP to different target groups.', distractors: { A: 'NLB operates at layer 4 — it routes based on IP/port, not URL paths.', C: 'CLB is legacy and doesn\'t support path-based routing.', D: 'GWLB routes to network appliances for inspection — not for application path routing.' } },
  { id: 'd2-elb-002', domain: 2, topics: ['ELB', 'NLB', 'TCP'], type: 'single', stem: 'An application requires extremely high throughput, ultra-low latency, and needs to preserve the client\'s source IP at the TCP level. Which load balancer type is appropriate?', options: [{ id: 'A', text: 'Application Load Balancer' }, { id: 'B', text: 'Network Load Balancer' }, { id: 'C', text: 'Classic Load Balancer' }, { id: 'D', text: 'Amazon API Gateway' }], correct: ['B'], explanation: 'NLB operates at layer 4 (TCP/UDP/TLS). It handles millions of requests per second with the lowest latency of any ELB type. It preserves the client source IP natively (unlike ALB which uses X-Forwarded-For).', distractors: { A: 'ALB has higher latency due to layer 7 processing — not optimal for ultra-low latency applications.', C: 'CLB is legacy — NLB is the modern high-performance choice.', D: 'API Gateway is for RESTful APIs with built-in auth/throttling — not a general load balancer.' } },
  { id: 'd2-elb-003', domain: 2, topics: ['ALB', 'HTTPS', 'SSL termination'], type: 'single', stem: 'An ALB receives HTTPS traffic from clients. The backend EC2 instances should receive HTTP traffic. Where is TLS terminated?', options: [{ id: 'A', text: 'On the EC2 instance (end-to-end encryption)' }, { id: 'B', text: 'At the ALB — the ALB terminates TLS and forwards HTTP to backends' }, { id: 'C', text: 'At the VPC boundary' }, { id: 'D', text: 'CloudFront must terminate TLS before ALB' }], correct: ['B'], explanation: 'This is TLS offloading/termination. The ALB handles the TLS handshake with clients, decrypts the traffic, and forwards plain HTTP to backends. This reduces CPU load on EC2 instances.', distractors: { A: 'End-to-end encryption would require HTTPS between ALB and backends — the question describes HTTP backends.', C: 'VPC boundary does not terminate TLS.', D: 'CloudFront is optional — ALB can terminate TLS directly.' } },
  { id: 'd2-elb-004', domain: 2, topics: ['ALB', 'sticky sessions', 'cookies'], type: 'single', stem: 'An application stores session state in memory on individual EC2 instances. To prevent users from losing sessions when requests are routed to different instances, what feature should be enabled?', options: [{ id: 'A', text: 'ALB connection multiplexing' }, { id: 'B', text: 'ALB sticky sessions (session affinity) — routes user requests to the same instance using cookies' }, { id: 'C', text: 'Target group deregistration delay' }, { id: 'D', text: 'Host-based routing' }], correct: ['B'], explanation: 'Sticky sessions use a cookie (AWSALB) to route a user\'s requests to the same target. However, this reduces load distribution — the better long-term solution is to store sessions in ElastiCache or DynamoDB.', distractors: { A: 'Connection multiplexing is an ALB connection optimization — not session routing.', C: 'Deregistration delay handles graceful shutdowns — not session routing.', D: 'Host-based routing routes by hostname — not by session.' } },
  { id: 'd2-elb-005', domain: 2, topics: ['ALB', 'WAF', 'integration'], type: 'single', stem: 'A company wants to add WAF protection to their ALB. What is the correct integration?', options: [{ id: 'A', text: 'Deploy WAF on EC2 instances behind the ALB' }, { id: 'B', text: 'Associate a WAF web ACL directly with the ALB' }, { id: 'C', text: 'WAF only integrates with CloudFront' }, { id: 'D', text: 'Use Security Groups to implement WAF rules' }], correct: ['B'], explanation: 'AWS WAF can be associated with ALBs, API Gateway, CloudFront, and AppSync. When associated with an ALB, WAF inspects requests before they reach the ALB listener rules.', distractors: { A: 'WAF is a managed service — it doesn\'t run on EC2 instances behind the ALB.', C: 'WAF integrates with ALB, API Gateway, and CloudFront — not CloudFront only.', D: 'Security groups filter by IP/port — they cannot implement WAF-level rule inspection.' } },
  { id: 'd2-elb-006', domain: 2, topics: ['ELB', 'access logs'], type: 'single', stem: 'A company needs to analyze all requests made to their ALB for compliance and troubleshooting. Where should ALB access logs be delivered?', options: [{ id: 'A', text: 'CloudTrail' }, { id: 'B', text: 'Amazon S3 — ALB access logs are delivered to S3 buckets' }, { id: 'C', text: 'CloudWatch Logs directly' }, { id: 'D', text: 'VPC Flow Logs' }], correct: ['B'], explanation: 'ALB access logs are delivered to S3 in compressed format every 5 minutes. They include detailed request information: time, client IP, request path, backend response time, etc. Can be analyzed with Athena.', distractors: { A: 'CloudTrail logs API calls (creating/modifying ALBs) — not individual HTTP requests.', C: 'ALB access logs don\'t deliver directly to CloudWatch Logs — they go to S3.', D: 'VPC Flow Logs capture network traffic metadata — not HTTP request details.' } },
  { id: 'd2-elb-007', domain: 2, topics: ['NLB', 'static IP', 'Elastic IP'], type: 'single', stem: 'A company needs their load balancer to have a static IP address that their firewall partners can whitelist. Which load balancer type supports static IPs per AZ?', options: [{ id: 'A', text: 'Application Load Balancer' }, { id: 'B', text: 'Network Load Balancer — supports Elastic IP assignment per AZ' }, { id: 'C', text: 'Classic Load Balancer' }, { id: 'D', text: 'All ELB types support static IPs' }], correct: ['B'], explanation: 'NLB supports assigning Elastic IP addresses per Availability Zone. This provides static, predictable IPs that can be whitelisted by external firewalls. ALBs use dynamic IPs behind a DNS name.', distractors: { A: 'ALB IPs are dynamic — DNS-based only. Static IPs are not supported.', C: 'CLB IPs are dynamic — not assignable as Elastic IPs.', D: 'Only NLB supports Elastic IP assignment.' } },
  { id: 'd2-elb-008', domain: 2, topics: ['ALB', 'target groups', 'types'], type: 'single', stem: 'An ALB needs to forward requests to: EC2 instances, Lambda functions, and fixed IP addresses. Which target group types support these? (Select TWO)', options: [{ id: 'A', text: 'Instance target group (for EC2)' }, { id: 'B', text: 'IP target group (for EC2 by IP, on-premises servers, Lambda)' }, { id: 'C', text: 'Lambda target group' }, { id: 'D', text: 'DNS target group' }, { id: 'E', text: 'ASG target group' }], correct: ['A', 'C'], explanation: 'ALB supports three target group types: Instance (registers EC2 instance IDs), IP (registers IP addresses — works for EC2, on-premises via Direct Connect/VPN, containers), and Lambda (invokes Lambda functions). C is correct for Lambda; A for EC2 instances.', distractors: { B: 'IP target groups exist but Lambda needs its own Lambda target group type. IP is for IP addresses, not Lambda.', D: 'DNS target groups don\'t exist in ALB.', E: 'ASG target groups don\'t exist — ASG registers instances into an existing ALB instance target group.' } },
  { id: 'd2-elb-009', domain: 2, topics: ['ALB', 'HTTP/2', 'gRPC'], type: 'single', stem: 'A microservices application uses gRPC for service-to-service communication. Which load balancer natively supports gRPC routing?', options: [{ id: 'A', text: 'Network Load Balancer' }, { id: 'B', text: 'Application Load Balancer — supports gRPC as a target group protocol' }, { id: 'C', text: 'Classic Load Balancer' }, { id: 'D', text: 'Gateway Load Balancer' }], correct: ['B'], explanation: 'ALB supports gRPC as a target group protocol (when the listener is HTTPS/HTTP2). It can route gRPC requests based on path and headers, supporting advanced gRPC use cases.', distractors: { A: 'NLB passes through TCP but doesn\'t understand gRPC-specific routing.', C: 'CLB is legacy and doesn\'t support gRPC.', D: 'GWLB is for network appliance integration — not gRPC routing.' } },
  { id: 'd2-elb-010', domain: 2, topics: ['ALB', 'redirect', 'rules'], type: 'single', stem: 'An ALB listener on port 80 (HTTP) should redirect all requests to HTTPS (port 443). How is this configured?', options: [{ id: 'A', text: 'Configure the EC2 instances to redirect HTTP to HTTPS' }, { id: 'B', text: 'Add a listener rule on port 80 with action Redirect to HTTPS' }, { id: 'C', text: 'HTTP-to-HTTPS redirect is automatic for ALBs' }, { id: 'D', text: 'Add a NACL rule redirecting port 80 to 443' }], correct: ['B'], explanation: 'ALB listener rules support a Redirect action that returns a 301 or 302 redirect to the client. A rule on the HTTP:80 listener redirecting to HTTPS:443 is the standard way to enforce HTTPS.', distractors: { A: 'Instance-level redirect works but requires configuration on every instance — ALB listener rules are more efficient.', C: 'HTTP-to-HTTPS redirect is not automatic — it must be configured.', D: 'NACLs forward packets — they cannot issue HTTP redirects.' } },
  { id: 'd2-elb-011', domain: 2, topics: ['ELB', 'cross-zone load balancing'], type: 'single', stem: 'An ALB has 2 instances in us-east-1a and 8 instances in us-east-1b. Cross-zone load balancing is enabled. How is traffic distributed?', options: [{ id: 'A', text: 'Traffic is distributed equally per AZ: 50% to us-east-1a, 50% to us-east-1b' }, { id: 'B', text: 'Traffic is distributed equally across all 10 instances regardless of AZ (10% each)' }, { id: 'C', text: 'Traffic is distributed based on instance capacity' }, { id: 'D', text: 'Traffic only goes to the AZ with more instances (us-east-1b)' }], correct: ['B'], explanation: 'Cross-zone load balancing distributes requests evenly across all registered targets in all enabled AZs — each of the 10 instances gets ~10% of traffic, regardless of which AZ they are in.', distractors: { A: 'Without cross-zone LB, traffic would be 50/50 per AZ. With cross-zone, it\'s per-instance.', C: 'ALB distributes round-robin by default — not based on instance capacity (that would require least outstanding requests algorithm).', D: 'Cross-zone means distribution across all AZs and instances, not only the most populated AZ.' } },
  { id: 'd2-elb-012', domain: 2, topics: ['NLB', 'health checks', 'protocol'], type: 'single', stem: 'An NLB target group has EC2 instances running a TCP-based application. What health check protocols are available for NLB target groups?', options: [{ id: 'A', text: 'HTTP and HTTPS only' }, { id: 'B', text: 'TCP, HTTP, HTTPS, and TLS' }, { id: 'C', text: 'TCP only' }, { id: 'D', text: 'UDP only' }], correct: ['B'], explanation: 'NLB health checks support TCP (successful connection), HTTP (checks HTTP response code), HTTPS (checks HTTPS response code), and TLS protocols. This allows health checking applications that require different protocols.', distractors: { A: 'HTTP/HTTPS are available but NLB also supports TCP and TLS health checks.', C: 'TCP is one option but not the only one for NLB health checks.', D: 'UDP is used for NLB traffic but not for health checks.' } },
  { id: 'd2-elb-013', domain: 2, topics: ['ALB', 'authentication', 'Cognito'], type: 'single', stem: 'A web application needs users to authenticate before accessing backend services. The authentication should happen at the load balancer layer without custom code. What ALB feature enables this?', options: [{ id: 'A', text: 'ALB security groups with IP-based restrictions' }, { id: 'B', text: 'ALB built-in authentication with Cognito User Pools or OIDC identity providers' }, { id: 'C', text: 'Implement authentication in a Lambda function behind ALB' }, { id: 'D', text: 'Use WAF to enforce authentication' }], correct: ['B'], explanation: 'ALB natively integrates with Cognito User Pools and OIDC providers (Google, Okta, etc.). The authenticate-cognito or authenticate-oidc listener rule action intercepts requests, handles OAuth2/OIDC flow, and only forwards authenticated requests to backends.', distractors: { A: 'Security groups filter by IP — not authentication.', C: 'Lambda-based authentication works but requires custom code — ALB native auth is simpler.', D: 'WAF provides web application firewall capabilities — not user authentication.' } },
  { id: 'd2-elb-014', domain: 2, topics: ['ALB', 'request routing', 'weighted'], type: 'single', stem: 'A company wants to gradually roll out a new application version to 10% of users while keeping 90% on the current version. What ALB feature enables this?', options: [{ id: 'A', text: 'ALB cannot route traffic by percentage' }, { id: 'B', text: 'Weighted target groups on the same ALB listener rule' }, { id: 'C', text: 'Use separate load balancers with Route 53 weighted routing' }, { id: 'D', text: 'Configure sticky sessions to stick 10% of users to the new version' }], correct: ['B'], explanation: 'ALB listener rules support weighted target groups. A forward action can specify multiple target groups with different weights (e.g., 90 to current, 10 to new). Traffic is distributed proportionally — perfect for canary deployments.', distractors: { A: 'ALB weighted target groups enable percentage-based routing.', C: 'Route 53 weighted routing works but requires two load balancers — ALB weighted target groups are simpler.', D: 'Sticky sessions bind users to specific instances — not to percentage-based target group splitting.' } },
  { id: 'd2-elb-015', domain: 2, topics: ['ALB', 'internal', 'external'], type: 'single', stem: 'An application tier needs to receive traffic from a web tier but should not be directly accessible from the internet. What type of load balancer should front the application tier?', options: [{ id: 'A', text: 'Internet-facing ALB in a public subnet' }, { id: 'B', text: 'Internal ALB in a private subnet' }, { id: 'C', text: 'ALB type doesn\'t affect internet accessibility' }, { id: 'D', text: 'NLB for internal traffic' }], correct: ['B'], explanation: 'Internal ALBs are not reachable from the internet — they only resolve to private IP addresses within the VPC. They are placed in private subnets and suitable for service-to-service communication.', distractors: { A: 'Internet-facing ALBs have public DNS names and are reachable from the internet.', C: 'The internet-facing vs internal designation directly affects internet accessibility.', D: 'NLB could be used internally too, but ALB is appropriate for HTTP-based microservices communication.' } },
  { id: 'd2-elb-016', domain: 2, topics: ['ELB', 'health check', 'thresholds'], type: 'single', stem: 'An ALB health check is marking instances as unhealthy when they are actually healthy during startup. The startup takes 60 seconds. What is the appropriate fix?', options: [{ id: 'A', text: 'Increase the health check interval to 5 minutes' }, { id: 'B', text: 'Increase the healthy threshold count and/or add an initial delay (health check grace period in ASG)' }, { id: 'C', text: 'Disable health checks temporarily' }, { id: 'D', text: 'Change health check protocol to TCP' }], correct: ['B'], explanation: 'The ASG health check grace period delays health checks after instance launch. Additionally, requiring more consecutive healthy responses before marking healthy (healthy threshold) allows the application time to start responding correctly.', distractors: { A: 'Increasing the interval to 5 minutes would make unhealthy detection too slow.', C: 'Disabling health checks removes failover capability — adjust the grace period instead.', D: 'TCP health checks just test connectivity — they don\'t wait for the application to start.' } },
  { id: 'd2-elb-017', domain: 2, topics: ['ALB', 'host-based routing', 'multi-domain'], type: 'single', stem: 'A single ALB needs to serve multiple websites: api.example.com and app.example.com, routing to different target groups. What ALB feature enables this?', options: [{ id: 'A', text: 'Path-based routing rules' }, { id: 'B', text: 'Host-based routing rules — routes based on the HTTP Host header' }, { id: 'C', text: 'Multiple ALBs, one per domain' }, { id: 'D', text: 'Weighted target groups' }], correct: ['B'], explanation: 'ALB listener rules can match the HTTP Host header. Requests to api.example.com and app.example.com hit the same ALB but are routed to different target groups based on the host name.', distractors: { A: 'Path-based routing uses URL paths — not hostnames.', C: 'Multiple ALBs per domain works but is more expensive — host-based routing shares one ALB.', D: 'Weighted target groups split traffic proportionally — not by hostname.' } },
  { id: 'd2-elb-018', domain: 2, topics: ['ALB', 'Lambda', 'serverless'], type: 'single', stem: 'An ALB listener rule needs to call a Lambda function directly for a specific URL path. How is this configured?', options: [{ id: 'A', text: 'ALB cannot invoke Lambda functions' }, { id: 'B', text: 'Create a Lambda target group and reference it in the ALB listener rule forward action' }, { id: 'C', text: 'Use API Gateway as an intermediary between ALB and Lambda' }, { id: 'D', text: 'Configure an SNS trigger on the Lambda function' }], correct: ['B'], explanation: 'ALB supports Lambda target groups. The listener rule forwards matching requests to the Lambda target group, invoking the Lambda function with an event containing the HTTP request details.', distractors: { A: 'ALB Lambda target groups are a supported feature.', C: 'API Gateway as an intermediary is unnecessary when ALB Lambda target groups are available.', D: 'SNS triggers Lambda asynchronously — ALB Lambda integration is synchronous (request-response).' } },
  { id: 'd2-elb-019', domain: 2, topics: ['NLB', 'TLS termination'], type: 'single', stem: 'An NLB listener is configured for TLS termination. What benefit does this provide over TCP passthrough?', options: [{ id: 'A', text: 'Better performance — NLB TLS is faster than TCP' }, { id: 'B', text: 'The NLB decrypts TLS, so backend instances receive plain TCP and don\'t need TLS certificates' }, { id: 'C', text: 'TLS termination at NLB adds content-based routing capability' }, { id: 'D', text: 'NLB TLS termination allows WAF integration' }], correct: ['B'], explanation: 'NLB TLS termination offloads TLS decryption to the load balancer. Backend instances receive plaintext TCP, simplifying certificate management on backends. ACM certificates are used at the NLB.', distractors: { A: 'TLS termination doesn\'t improve throughput performance for the backend.', C: 'NLB remains layer 4 even with TLS termination — no content-based routing.', D: 'WAF integrates with ALBs, not NLBs.' } },
  { id: 'd2-elb-020', domain: 2, topics: ['ALB', 'connection draining', 'deregistration'], type: 'single', stem: 'When an EC2 instance is deregistered from an ALB target group, what happens to in-flight requests?', options: [{ id: 'A', text: 'In-flight requests are immediately dropped' }, { id: 'B', text: 'ALB waits for in-flight requests to complete during the deregistration delay period (default 300 seconds)' }, { id: 'C', text: 'In-flight requests are re-routed to other instances' }, { id: 'D', text: 'The ALB sends a 503 for all new and in-flight requests' }], correct: ['B'], explanation: 'When an instance deregisters, ALB stops sending new requests but allows in-flight requests to complete up to the deregistration delay timeout. This ensures graceful connection draining.', distractors: { A: 'Deregistration delay prevents immediate dropping of in-flight requests.', C: 'In-flight requests are not re-routed — they complete on the deregistering instance.', D: '503 is returned only after the deregistration delay expires for any remaining connections.' } },
  { id: 'd2-elb-021', domain: 2, topics: ['ALB', 'X-Forwarded-For'], type: 'single', stem: 'A web application behind an ALB needs to know the original client IP address. The application receives the ALB IP in the REMOTE_ADDR. How can the client IP be obtained?', options: [{ id: 'A', text: 'Configure the ALB to preserve the client IP in REMOTE_ADDR' }, { id: 'B', text: 'Read the X-Forwarded-For HTTP header added by the ALB' }, { id: 'C', text: 'Use the X-Real-IP header' }, { id: 'D', text: 'Client IP is not available when using an ALB' }], correct: ['B'], explanation: 'ALB appends the client\'s original IP to the X-Forwarded-For header. The application can read this header to get the client IP. ALBs also add X-Forwarded-Proto and X-Forwarded-Port.', distractors: { A: 'ALB terminates connections — the connection from ALB to EC2 comes from the ALB\'s IP.', C: 'X-Real-IP is not an AWS-standard header — ALB uses X-Forwarded-For.', D: 'Client IP is available in X-Forwarded-For.' } },
  { id: 'd2-elb-022', domain: 2, topics: ['ELB', 'SNI', 'multiple certificates'], type: 'single', stem: 'An ALB serves multiple HTTPS domains. Each domain has its own certificate. How can multiple SSL/TLS certificates be served from a single ALB listener?', options: [{ id: 'A', text: 'Create a separate ALB per domain' }, { id: 'B', text: 'Add multiple certificates to the HTTPS listener using SNI (Server Name Indication)' }, { id: 'C', text: 'Use a wildcard certificate for all domains' }, { id: 'D', text: 'Each ALB listener only supports one certificate' }], correct: ['B'], explanation: 'ALB supports SNI, allowing multiple TLS certificates to be associated with a single HTTPS listener. The ALB presents the appropriate certificate based on the hostname in the client\'s TLS hello.', distractors: { A: 'Separate ALBs per domain is more expensive and harder to manage.', C: 'Wildcard certificates only cover one level of subdomain — not multiple separate domains.', D: 'ALB supports multiple certificates per HTTPS listener via SNI.' } },
  { id: 'd2-elb-023', domain: 2, topics: ['ALB', 'response codes', 'health check'], type: 'single', stem: 'An ALB health check is configured with a success code of 200. The application returns 302 for healthy responses. The instances are being marked unhealthy. What should be done?', options: [{ id: 'A', text: 'Configure the application to return 200 instead' }, { id: 'B', text: 'Update the health check success codes to include 200-399 or specifically 302' }, { id: 'C', text: 'Use TCP health checks instead' }, { id: 'D', text: 'Enable sticky sessions' }], correct: ['B'], explanation: 'ALB health check success codes can be configured to accept a range (e.g., 200-399) or specific codes. If the application legitimately returns 302, update the health check matcher to accept 302.', distractors: { A: 'Changing application behavior to match the health check is not always feasible.', C: 'TCP health checks only verify connectivity — they don\'t check HTTP response codes.', D: 'Sticky sessions are for session routing — not health check configuration.' } },
  { id: 'd2-elb-024', domain: 2, topics: ['ELB', 'cross-zone', 'NLB cost'], type: 'single', stem: 'An NLB has cross-zone load balancing enabled. Traffic between AZs incurs additional cost. When is cross-zone load balancing free for NLB?', options: [{ id: 'A', text: 'NLB cross-zone load balancing is always free' }, { id: 'B', text: 'Cross-zone load balancing for NLB incurs inter-AZ data transfer charges — unlike ALB where it is free by default' }, { id: 'C', text: 'Cross-zone is free for NLB only if targets are Lambda functions' }, { id: 'D', text: 'Cross-zone is free when using Elastic IPs on NLB' }], correct: ['B'], explanation: 'ALB has cross-zone load balancing enabled by default with no inter-AZ data transfer charges. NLB has cross-zone disabled by default, and enabling it incurs inter-AZ data transfer charges.', distractors: { A: 'NLB cross-zone load balancing does incur inter-AZ data transfer charges.', C: 'Cross-zone cost applies regardless of target type.', D: 'Elastic IPs affect inbound routing — not cross-zone data transfer costs.' } },
  { id: 'd2-elb-025', domain: 2, topics: ['ALB', 'listener rules', 'priority'], type: 'single', stem: 'An ALB has two listener rules: Rule 1 (priority 10): path /api/* → API target group. Rule 2 (priority 20): path /* → Web target group. A request arrives for /api/users. Which rule matches?', options: [{ id: 'A', text: 'Rule 2 — it is more specific' }, { id: 'B', text: 'Rule 1 — lower priority number is evaluated first' }, { id: 'C', text: 'Both rules match — traffic is distributed 50/50' }, { id: 'D', text: 'The last matching rule wins' }], correct: ['B'], explanation: 'ALB evaluates listener rules in ascending priority order (lowest number = highest priority). The first matching rule is applied. Rule 1 (priority 10) evaluates first and matches /api/users, so it routes to the API target group.', distractors: { A: 'Priority number, not specificity, determines evaluation order.', C: 'Only the first matching rule is applied — not both.', D: 'First matching rule wins in ALB — not the last.' } },
  { id: 'd2-elb-026', domain: 2, topics: ['ELB', 'connection timeout'], type: 'single', stem: 'A backend service sometimes takes longer than the default ALB idle timeout to process requests. Clients are receiving 504 gateway timeout errors. What should be adjusted?', options: [{ id: 'A', text: 'Increase the ALB idle timeout (default 60 seconds)' }, { id: 'B', text: 'Use an NLB instead of ALB for long-running requests' }, { id: 'C', text: 'Enable sticky sessions' }, { id: 'D', text: 'Increase the health check timeout' }], correct: ['A'], explanation: 'The ALB idle timeout (default 60 seconds) closes connections that are idle for that duration. For long-running requests (file processing, reports), increase this value. Maximum is 4,000 seconds.', distractors: { B: 'NLB is for layer 4 requirements — changing load balancer types is not the solution for timeout tuning.', C: 'Sticky sessions affect routing — not timeouts.', D: 'Health check timeout affects how long to wait for a health check response — not request processing time.' } },
  { id: 'd2-elb-027', domain: 2, topics: ['ALB', 'target group', 'slow start'], type: 'single', stem: 'Newly registered targets behind an ALB receive full traffic immediately and are overwhelmed during warmup. What feature gradually increases traffic to new targets?', options: [{ id: 'A', text: 'Health check grace period' }, { id: 'B', text: 'Target group slow start mode — gradually increases request rate to new targets' }, { id: 'C', text: 'Increase the deregistration delay' }, { id: 'D', text: 'Use sticky sessions for new targets' }], correct: ['B'], explanation: 'Slow start mode is configured on the target group. When a target is first registered, it receives a linearly increasing share of requests over the slow start duration (30-900 seconds), reducing warmup overwhelm.', distractors: { A: 'Health check grace period delays when health checks start — not when traffic is ramped up.', C: 'Deregistration delay is for de-registration — not registration traffic ramp-up.', D: 'Sticky sessions bind specific users to a target — not a traffic ramping mechanism.' } },
  { id: 'd2-elb-028', domain: 2, topics: ['ELB', 'ALB', 'HTTPS', 'ACM'], type: 'single', stem: 'A company wants to use ACM certificates on their ALB with automatic renewal. What is required?', options: [{ id: 'A', text: 'Manually renew ACM certificates annually and update the ALB' }, { id: 'B', text: 'Request a certificate in ACM, associate it with the ALB HTTPS listener — ACM handles renewal automatically' }, { id: 'C', text: 'ACM certificates cannot be used with ALBs' }, { id: 'D', text: 'Export the ACM certificate and install it on the EC2 instances' }], correct: ['B'], explanation: 'ACM public certificates integrated with ALBs renew automatically before expiry. No manual renewal is needed. The certificate is directly associated with the ALB listener — no installation on EC2 instances required.', distractors: { A: 'ACM handles automatic renewal — no manual renewal needed.', C: 'ACM is fully integrated with ALB and NLB.', D: 'ACM certificates are used at the load balancer level — not exported to EC2 instances.' } },
  { id: 'd2-elb-029', domain: 2, topics: ['ALB', 'least outstanding requests', 'routing algorithm'], type: 'single', stem: 'An ALB target group has instances that process requests at very different speeds. Some requests take 100ms, others take 30 seconds. What load balancing algorithm prevents slow-processing instances from becoming overloaded?', options: [{ id: 'A', text: 'Round robin (default)' }, { id: 'B', text: 'Least outstanding requests — routes new requests to the target with the fewest in-flight requests' }, { id: 'C', text: 'Sticky sessions' }, { id: 'D', text: 'IP hash routing' }], correct: ['B'], explanation: 'Least outstanding requests routing sends new requests to the target with the fewest active requests. For heterogeneous request durations, this prevents a single slow-processing target from accumulating a long queue while others are idle.', distractors: { A: 'Round robin distributes equally without considering current load — slow instances accumulate backlog.', C: 'Sticky sessions route by user session — not by target load.', D: 'ALB doesn\'t have an IP hash routing algorithm as a named option.' } },
  { id: 'd2-elb-030', domain: 2, topics: ['NLB', 'preserve source IP'], type: 'single', stem: 'An NLB forwards traffic to EC2 instances. The EC2 instances\' security groups need to allow the original client IP addresses for inbound rules. What is the key difference from ALB?', options: [{ id: 'A', text: 'NLB replaces the source IP with its own IP — configure security groups to allow the NLB IP' }, { id: 'B', text: 'NLB preserves the original client source IP — security groups should allow the expected client CIDR ranges' }, { id: 'C', text: 'There is no difference — both ALB and NLB behave the same with source IPs' }, { id: 'D', text: 'NLB uses X-Forwarded-For for client IP like ALB' }], correct: ['B'], explanation: 'NLB preserves the original client source IP at the TCP level. EC2 instance security groups see the client\'s actual IP. ALB replaces the source IP with its own and adds X-Forwarded-For for the original.', distractors: { A: 'This describes ALB behavior — NLB preserves the original IP.', C: 'There is a fundamental difference — NLB preserves IP, ALB replaces it.', D: 'X-Forwarded-For is an HTTP header — NLB is layer 4 and doesn\'t add HTTP headers.' } },
  { id: 'd2-elb-031', domain: 2, topics: ['ALB', 'Global Accelerator', 'static IP'], type: 'single', stem: 'A company uses ALB but partner firewalls require static IPs. ALB doesn\'t support static IPs. What is the recommended solution?', options: [{ id: 'A', text: 'Switch to NLB which supports static IPs' }, { id: 'B', text: 'Put AWS Global Accelerator in front of ALB — Global Accelerator provides static Anycast IPs' }, { id: 'C', text: 'Use a fixed CNAME for the ALB' }, { id: 'D', text: 'ALB actually does support Elastic IPs' }], correct: ['B'], explanation: 'AWS Global Accelerator provides two static Anycast IP addresses that stay constant. Behind these IPs, you can use ALB (or other endpoints). Firewalls whitelist the static Global Accelerator IPs while ALB continues to handle HTTP routing.', distractors: { A: 'Switching to NLB loses ALB\'s layer 7 features (path routing, host routing, auth, etc.).', C: 'A fixed CNAME still resolves to dynamic IPs — not useful for IP whitelisting.', D: 'ALB does not support Elastic IPs.' } },
  { id: 'd2-elb-032', domain: 2, topics: ['ALB', 'WebSocket', 'upgrade'], type: 'single', stem: 'A real-time application uses WebSocket connections. Which load balancer supports WebSocket upgrades?', options: [{ id: 'A', text: 'Classic Load Balancer only' }, { id: 'B', text: 'Application Load Balancer supports WebSocket natively' }, { id: 'C', text: 'Network Load Balancer only' }, { id: 'D', text: 'WebSocket is not supported by any ELB type' }], correct: ['B'], explanation: 'ALB natively supports WebSocket connections. When a client sends an HTTP Upgrade request, ALB passes through the upgrade to the backend and maintains the persistent WebSocket connection.', distractors: { A: 'CLB is legacy — ALB is the modern choice.', C: 'NLB passes through TCP but doesn\'t specifically handle WebSocket upgrades at the application layer.', D: 'ALB fully supports WebSocket.' } },
  { id: 'd2-elb-033', domain: 2, topics: ['ELB', 'logs', 'analysis'], type: 'single', stem: 'A company wants to analyze ALB access logs to find the top 10 slowest API endpoints. ALB logs are in S3. What is the recommended approach?', options: [{ id: 'A', text: 'Download logs manually and analyze with scripts' }, { id: 'B', text: 'Use Amazon Athena to query the ALB logs in S3 using the predefined ALB log table schema' }, { id: 'C', text: 'Load logs into RDS and write SQL queries' }, { id: 'D', text: 'Use CloudWatch Logs Insights' }], correct: ['B'], explanation: 'Amazon Athena can query ALB access logs directly from S3 using a predefined table DDL available in the AWS documentation. No data loading is needed — Athena queries logs in place with standard SQL.', distractors: { A: 'Manual download and scripting is slow and not scalable for large log volumes.', C: 'Loading to RDS requires ETL pipelines — Athena queries in-place from S3.', D: 'CloudWatch Logs Insights queries CloudWatch Logs, not S3-stored ALB access logs.' } },
  { id: 'd2-elb-034', domain: 2, topics: ['ALB', 'request tracing'], type: 'single', stem: 'A developer needs to trace individual requests through an ALB to specific backend instances for troubleshooting. What header does ALB add for this purpose?', options: [{ id: 'A', text: 'X-Request-ID' }, { id: 'B', text: 'X-Amzn-Trace-Id — a unique trace ID added to each request by ALB' }, { id: 'C', text: 'X-Correlation-ID' }, { id: 'D', text: 'ALB does not add request tracing headers' }], correct: ['B'], explanation: 'ALB adds the X-Amzn-Trace-Id header to each request. This ID can be used to correlate requests in ALB access logs, X-Ray traces, and application logs for end-to-end request tracing.', distractors: { A: 'X-Request-ID is a common convention but not the specific header added by ALB.', C: 'X-Correlation-ID is a general convention — not the AWS-specific header.', D: 'ALB does add X-Amzn-Trace-Id for tracing.' } },
  { id: 'd2-elb-035', domain: 2, topics: ['ALB', 'fixed response'], type: 'single', stem: 'An ALB should return a custom maintenance page (HTTP 503) for a specific URL path without involving backend instances. What ALB action achieves this?', options: [{ id: 'A', text: 'Forward action with a maintenance target group' }, { id: 'B', text: 'Fixed-response action on the listener rule — returns a specified status code and body without backend involvement' }, { id: 'C', text: 'Redirect action to a maintenance page' }, { id: 'D', text: 'Authenticate action blocking the path' }], correct: ['B'], explanation: 'The ALB fixed-response action returns a configurable HTTP status code, content type, and message body directly from the load balancer. No backend instances are involved — useful for maintenance mode or static error pages.', distractors: { A: 'A maintenance target group still requires backend instances.', C: 'Redirect sends clients to a different URL — fixed-response returns content directly.', D: 'Authenticate action is for authentication flow — not custom responses.' } },
  { id: 'd2-elb-036', domain: 2, topics: ['ELB', 'IPv6', 'dualstack'], type: 'single', stem: 'A company wants their ALB to accept both IPv4 and IPv6 requests. What configuration is needed?', options: [{ id: 'A', text: 'Create separate ALBs for IPv4 and IPv6' }, { id: 'B', text: 'Enable dualstack mode on the ALB — it receives a dualstack DNS name and accepts both IPv4 and IPv6' }, { id: 'C', text: 'ALBs only support IPv4' }, { id: 'D', text: 'IPv6 requires a Network Load Balancer' }], correct: ['B'], explanation: 'ALB dualstack mode assigns both an IPv4 and IPv6 address. The load balancer gets a dualstack DNS name (e.g., dualstack.my-alb.us-east-1.elb.amazonaws.com) that resolves to both address types.', distractors: { A: 'Separate ALBs for each IP version is unnecessary with dualstack.', C: 'ALBs support both IPv4 and IPv6 in dualstack mode.', D: 'Both ALB and NLB support dualstack/IPv6.' } },
  { id: 'd2-elb-037', domain: 2, topics: ['ALB', 'HTTPS', 'policy'], type: 'single', stem: 'A company needs to comply with a security policy requiring TLS 1.2 or higher. Old clients using TLS 1.0 should be rejected. What should be configured on the ALB?', options: [{ id: 'A', text: 'Add a WAF rule to reject TLS 1.0' }, { id: 'B', text: 'Select an ALB security policy that does not include TLS 1.0 cipher suites (e.g., ELBSecurityPolicy-TLS13-1-2-2021-06)' }, { id: 'C', text: 'ALB always supports all TLS versions' }, { id: 'D', text: 'Configure TLS policy in the EC2 security group' }], correct: ['B'], explanation: 'ALB HTTPS listeners use security policies that define which TLS versions and cipher suites are supported. Selecting a policy that requires TLS 1.2+ (like ELBSecurityPolicy-TLS13-1-2-2021-06) automatically rejects connections from older clients.', distractors: { A: 'WAF cannot filter based on TLS version — it operates after TLS is established.', C: 'ALB security policies control which TLS versions are accepted.', D: 'Security groups control network access — not TLS version policies.' } },
  { id: 'd2-elb-038', domain: 2, topics: ['ELB', 'deletion protection'], type: 'single', stem: 'A company wants to prevent accidental deletion of their production ALB. What feature protects against this?', options: [{ id: 'A', text: 'IAM policies restricting elasticloadbalancing:DeleteLoadBalancer' }, { id: 'B', text: 'Enable deletion protection on the ALB — it must be explicitly disabled before the ALB can be deleted' }, { id: 'C', text: 'ALBs cannot be deleted if they have active targets' }, { id: 'D', text: 'Use CloudFormation to prevent manual deletion' }], correct: ['B'], explanation: 'ALBs have a deletion protection attribute. When enabled, attempts to delete the load balancer return an error until protection is explicitly disabled. This prevents accidental deletion in production.', distractors: { A: 'IAM policies can restrict deletion but require per-user setup — deletion protection is a simpler resource-level safeguard.', C: 'ALBs can be deleted regardless of active targets (deletion protection must be off).', D: 'CloudFormation manages resources it creates but doesn\'t prevent AWS console deletions.' } },
  { id: 'd2-elb-039', domain: 2, topics: ['ALB', 'multi-value headers'], type: 'single', stem: 'An ALB routes requests to a Lambda function. The HTTP request has duplicate header names (multiple Cookie headers). How does ALB pass these to Lambda?', options: [{ id: 'A', text: 'ALB passes only the first header value, discarding duplicates' }, { id: 'B', text: 'Enable multi-value headers on the Lambda target group — ALB sends an array of values for duplicate headers' }, { id: 'C', text: 'Lambda cannot receive multi-value headers' }, { id: 'D', text: 'ALB concatenates duplicate headers with commas automatically' }], correct: ['B'], explanation: 'By default, ALB passes only one value per header to Lambda. With multi-value headers enabled on the Lambda target group, ALB passes all values as a JSON array, preserving multiple Cookie or query string values.', distractors: { A: 'This is the default single-value behavior — multi-value headers can be enabled to preserve duplicates.', C: 'Lambda can receive multi-value headers when the target group feature is enabled.', D: 'Comma concatenation is the default behavior for duplicate headers — multi-value headers use arrays.' } },
  { id: 'd2-elb-040', domain: 2, topics: ['ELB', 'zonal shift', 'ARC'], type: 'single', stem: 'An Availability Zone is experiencing degraded performance. A company wants to quickly shift all ALB traffic away from that AZ. What AWS service supports this without configuration changes?', options: [{ id: 'A', text: 'Modify ALB to remove the problematic AZ from its subnets' }, { id: 'B', text: 'AWS Application Recovery Controller (ARC) Zonal Shift — redirects traffic away from an AZ in minutes' }, { id: 'C', text: 'Update Route 53 health checks to detect the AZ issue' }, { id: 'D', text: 'Restart all instances in the affected AZ' }], correct: ['B'], explanation: 'ARC Zonal Shift allows routing traffic away from an impaired AZ in minutes with a single API call or console action. It works with ALB, NLB, and Auto Scaling, providing rapid AZ-level traffic isolation.', distractors: { A: 'Modifying ALB subnet configuration is a slower process requiring deployment changes.', C: 'Route 53 health checks respond to endpoint failures — not AZ-level degradation affecting only some requests.', D: 'Restarting instances in an affected AZ doesn\'t solve AZ-level infrastructure issues.' } },
  { id: 'd2-elb-041', domain: 2, topics: ['ALB', 'desync mitigation'], type: 'single', stem: 'An ALB shows security warnings about HTTP request smuggling attacks. What feature should be enabled to mitigate this?', options: [{ id: 'A', text: 'Enable WAF on the ALB' }, { id: 'B', text: 'Enable HTTP desync mitigation mode — ALB normalizes or rejects malformed HTTP requests' }, { id: 'C', text: 'Use HTTPS instead of HTTP' }, { id: 'D', text: 'Enable Shield Advanced' }], correct: ['B'], explanation: 'HTTP desync (request smuggling) attacks exploit inconsistencies in HTTP parsing. ALB desync mitigation mode (Strictest, Defensive, or Monitor) normalizes or rejects malformed HTTP requests that could be used for smuggling.', distractors: { A: 'WAF can detect some request smuggling patterns but desync mitigation is specifically designed for this attack class.', C: 'HTTPS encrypts traffic but doesn\'t prevent HTTP protocol-level desync attacks.', D: 'Shield Advanced protects against DDoS — not HTTP request smuggling.' } },
  { id: 'd2-elb-042', domain: 2, topics: ['ELB', 'Gateway Load Balancer', 'use case'], type: 'single', stem: 'A company wants to route all VPC traffic through their existing third-party firewall appliances before it reaches applications. What load balancer type enables transparent appliance insertion?', options: [{ id: 'A', text: 'Application Load Balancer' }, { id: 'B', text: 'Network Load Balancer' }, { id: 'C', text: 'Gateway Load Balancer (GWLB)' }, { id: 'D', text: 'Classic Load Balancer' }], correct: ['C'], explanation: 'Gateway Load Balancer is purpose-built for routing traffic through third-party network appliances (firewalls, IDS/IPS). It uses GENEVE encapsulation for transparent traffic forwarding. Traffic looks unchanged to endpoints.', distractors: { A: 'ALB is for HTTP/HTTPS application load balancing — not network appliance insertion.', B: 'NLB is for TCP/UDP load balancing — not transparent network appliance insertion.', D: 'CLB is legacy — no support for appliance insertion.' } },
  { id: 'd2-elb-043', domain: 2, topics: ['ALB', 'rule conditions', 'query string'], type: 'single', stem: 'An ALB rule should route requests to a specific target group only when the query string contains ?beta=true. What condition type should be used in the listener rule?', options: [{ id: 'A', text: 'Path condition' }, { id: 'B', text: 'Host condition' }, { id: 'C', text: 'Query string condition — matches on specific query string key-value pairs' }, { id: 'D', text: 'HTTP method condition' }], correct: ['C'], explanation: 'ALB listener rules support query string conditions. A condition matching query string key=beta, value=true routes matching requests to the specified target group — useful for A/B testing or feature flag routing.', distractors: { A: 'Path conditions match URL path — not query string parameters.', B: 'Host conditions match the hostname/domain — not query string.', D: 'HTTP method conditions match GET/POST/PUT/etc. — not query string values.' } },
  { id: 'd2-elb-044', domain: 2, topics: ['ELB', 'VPC', 'subnets'], type: 'single', stem: 'An internet-facing ALB must be deployed in which type of subnet?', options: [{ id: 'A', text: 'Private subnets — for security' }, { id: 'B', text: 'Public subnets — subnets with a route to an Internet Gateway' }, { id: 'C', text: 'Any subnet — ALB handles internet routing automatically' }, { id: 'D', text: 'ALBs are deployed outside VPCs' }], correct: ['B'], explanation: 'Internet-facing ALBs must be deployed in public subnets (subnets with an Internet Gateway route). At least one public subnet per AZ where the ALB operates is required. Backend EC2 targets can be in private subnets.', distractors: { A: 'Internet-facing ALBs need public subnets — private subnets don\'t have internet routing for the ALB\'s public IP.', C: 'ALBs don\'t self-route — they depend on the subnet\'s route table for internet access.', D: 'ALBs are deployed within VPCs — they have ENIs in subnets.' } },
  { id: 'd2-elb-045', domain: 2, topics: ['ALB', 'mutual TLS', 'mTLS'], type: 'single', stem: 'A company requires that both the client and the server authenticate each other using certificates (mutual TLS). Which load balancer supports mTLS?', options: [{ id: 'A', text: 'Classic Load Balancer' }, { id: 'B', text: 'Application Load Balancer — supports mutual TLS with client certificate validation' }, { id: 'C', text: 'NLB only supports mTLS via passthrough' }, { id: 'D', text: 'mTLS requires a custom proxy solution' }], correct: ['B'], explanation: 'ALB supports mutual TLS (mTLS). It can validate client certificates against a trust store (CA certificate). Requests from clients with valid certificates are allowed; others are rejected. This is used for machine-to-machine API security.', distractors: { A: 'CLB is legacy and doesn\'t support mTLS.', C: 'NLB can pass through TLS (including mTLS) to backends, but ALB performs mTLS termination natively.', D: 'ALB natively supports mTLS — no custom proxy needed.' } },
]
