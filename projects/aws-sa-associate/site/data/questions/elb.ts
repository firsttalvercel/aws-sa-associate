import type { Question } from '../../lib/types'
export const elb_questions: Question[] = [
  { id: 'd2-elb-001', domain: 2, topics: ['ELB', 'ALB', 'layer 7'], type: 'single', stem: 'An application needs to route /api/* requests to one target group and /web/* to another. Which load balancer?', options: [{ id: 'A', text: 'Network Load Balancer' }, { id: 'B', text: 'Gateway Load Balancer' }, { id: 'C', text: 'Classic Load Balancer' }, { id: 'D', text: 'Application Load Balancer' }], correct: ['D'], explanation: 'ALB operates at Layer 7 (HTTP/HTTPS). Listener rules support path-based routing — route /api/* to API target group and /web/* to web target group. Fully configurable rule conditions and actions.', distractors: {
      A: 'NLB operates at Layer 4 — it routes by IP/port, not HTTP path. No path-based routing support.',
      C: 'Classic Load Balancer does not support content-based routing rules — it is feature-limited legacy.',
      B: 'GWLB routes to firewall appliances for inline inspection — it is not for application routing by URL path.'
    } },
  { id: 'd2-elb-002', domain: 2, topics: ['ELB', 'NLB', 'static IP'], type: 'single', stem: 'A client firewall whitelists a fixed IP for outbound connections to an AWS service. Which load balancer provides static IPs?', options: [{ id: 'A', text: 'Network Load Balancer' }, { id: 'B', text: 'Classic Load Balancer' }, { id: 'C', text: 'Both ALB and NLB' }, { id: 'D', text: 'Application Load Balancer' }], correct: ['A'], explanation: 'NLB provides static Elastic IPs per AZ. ALB IPs are dynamic — they change with scaling events. Use NLB when clients need a stable IP to whitelist.', distractors: {
      D: 'ALB IPs are dynamic and managed by AWS — you cannot assign Elastic IPs to an ALB.',
      B: 'Classic Load Balancer IPs are dynamic — they are not static or fixed.',
      C: 'Only NLB supports Elastic IPs — ALB does not support IP assignment.'
    } },
  { id: 'd2-elb-003', domain: 2, topics: ['ELB', 'ALB', 'host-based routing'], type: 'single', stem: 'An ALB serves api.example.com and www.example.com on the same port 443. How are they routed to different target groups?', options: [{ id: 'A', text: 'Path-based routing rules' }, { id: 'B', text: 'Create two ALBs' }, { id: 'C', text: 'Host-based routing rules' }, { id: 'D', text: 'Use weighted routing' }], correct: ['C'], explanation: 'ALB listener rules support host conditions: IF Host header == api.example.com THEN forward to API target group; IF Host header == www.example.com THEN forward to web target group.', distractors: {
      A: 'Path-based routing checks the URL path — host-based routing checks the HTTP Host header.',
      B: 'Two ALBs is unnecessary overhead — a single ALB with host-based routing handles multiple domains.',
      D: 'Weighted routing splits by percentage — it does not route different hostnames to different backends.'
    } },
  { id: 'd2-elb-004', domain: 2, topics: ['ELB', 'cross-zone load balancing'], type: 'single', stem: 'AZ-1 has 8 instances, AZ-2 has 2 instances. With cross-zone load balancing disabled, what does each instance receive?', options: [{ id: 'A', text: 'AZ-1 instances receive 6.25% each (50%/8); AZ-2 instances receive 25% each (50%/2)' }, { id: 'B', text: 'Each instance receives 10% of traffic' }, { id: 'C', text: 'All traffic goes to AZ-1' }, { id: 'D', text: 'AZ-1 instances receive 12.5% each; AZ-2 instances receive 2.5% each' }], correct: ['A'], explanation: 'Without cross-zone LB: each AZ receives 50% of traffic. AZ-1: 50%/8=6.25% per instance. AZ-2: 50%/2=25% per instance. AZ-2 instances are heavily over-loaded. Cross-zone LB evens this out.', distractors: {
      B: 'Equal per-instance distribution requires cross-zone load balancing to be enabled.',
      D: '12.5% per instance in AZ-1 and 2.5% in AZ-2 are incorrect — AZ-2 receives 50% shared by only 2 instances.',
      C: 'Traffic is split 50/50 by AZ regardless of instance count without cross-zone load balancing.'
    } },
  { id: 'd2-elb-005', domain: 2, topics: ['ELB', 'ALB', 'sticky sessions'], type: 'single', stem: 'An application stores session state locally on each EC2 instance. Users must be routed to the same instance for each request. Solution?', options: [{ id: 'A', text: 'ALB target group sticky sessions' }, { id: 'B', text: 'Move session state to ElastiCache' }, { id: 'C', text: 'Both A and C' }, { id: 'D', text: 'NLB TCP routing' }], correct: ['C'], explanation: 'Short-term fix: ALB sticky sessions (duration-based or application-based cookies). Long-term fix: externalize session state to ElastiCache so any instance can serve any user — better resilience and scale.', distractors: {
      A: 'Sticky sessions work but create uneven load and break when instances are terminated.',
      D: 'NLB connection hashing is for TCP flows, not HTTP session affinity — it does not address application session state.',
      B: 'Externalizing session state is architecturally correct but may not be an immediate option.'
    } },
  { id: 'd2-elb-006', domain: 2, topics: ['ELB', 'health checks'], type: 'single', stem: 'An ALB health check HTTP call returns 500 from a healthy instance due to a bug in the /health endpoint. What happens?', options: [{ id: 'A', text: 'The ALB marks the instance unhealthy and stops routing traffic to it' }, { id: 'B', text: 'The ALB retries 3 times before marking the instance unhealthy' }, { id: 'C', text: 'Nothing' }, { id: 'D', text: 'The ALB marks the instance healthy because HTTP 500 is a server response (not a connection failure)' }], correct: ['A'], explanation: 'ALB health checks evaluate the HTTP response code. By default, only 200 is considered healthy. A 500 response causes the instance to be marked unhealthy. Configure the healthy threshold range (200-499, etc.) to customize.', distractors: {
      D: 'Any HTTP response — including 500 — is evaluated against the success codes. 500 fails the default health check.',
      B: 'ALB uses a consecutive failure count (unhealthy threshold) not a single retry — but 500 counts as a failure each time.',
      C: '5xx responses cause health check failures — they do not pass health checks.'
    } },
  { id: 'd2-elb-007', domain: 2, topics: ['ELB', 'NLB', 'preserve source IP'], type: 'single', stem: 'Backend servers need to see the real client IP address. NLB is used. Does the backend see the original client IP?', options: [{ id: 'A', text: 'No' }, { id: 'B', text: 'Yes' }, { id: 'C', text: 'Only if the Proxy Protocol v2 header is configured' }, { id: 'D', text: 'No' }], correct: ['B'], explanation: 'NLB preserves the original client source IP for TCP/UDP traffic by default — the backend sees the actual client IP. For ALB, the source IP is the ALB node IP; use X-Forwarded-For for the original client IP.', distractors: { A: 'NLB preserves client IP natively — it does not masquerade as the NLB node IP for TCP passthrough.', C: 'Proxy Protocol v2 is for cases where NLB is in cross-account or private link scenarios — native TCP passthrough preserves client IP without it.', D: 'X-Forwarded-For is an ALB mechanism — NLB preserves source IP at the network layer without needing HTTP headers.' } },
  { id: 'd2-elb-008', domain: 2, topics: ['ELB', 'ALB', 'target types'], type: 'single', stem: 'An ALB target group can register which of the following as targets?', options: [{ id: 'A', text: 'EC2 instances and IP addresses' }, { id: 'B', text: 'EC2 instances only' }, { id: 'C', text: 'EC2 instances, IP addresses (private), Lambda functions, and ALB (for chaining)' }, { id: 'D', text: 'EC2 instances and Lambda functions only' }], correct: ['C'], explanation: 'ALB target group target types: instance (EC2 instance ID), ip (private IP address for containers, on-prem, peered VPC), and lambda. Lambda targets are for serverless applications behind an ALB.', distractors: {
      B: 'ALB supports IP and Lambda targets in addition to EC2 instances — it is not instance-only.',
      D: 'ALB supports IP-based targets — NLB is not required for IP-based targets.',
      A: 'Lambda targets are supported in the same ALB listener — no separate ALB is needed.'
    } },
  { id: 'd2-elb-009', domain: 2, topics: ['ELB', 'deregistration delay'], type: 'single', stem: 'Instances deregister from a target group when scale-in occurs. In-flight requests take up to 30 seconds. What prevents request failures?', options: [{ id: 'A', text: 'Sticky sessions' }, { id: 'B', text: 'Target group slow start' }, { id: 'C', text: 'Connection draining' }, { id: 'D', text: 'Health check grace period' }], correct: ['C'], explanation: 'Connection draining (deregistration delay): when an instance deregisters, ALB stops routing new requests to it but allows in-flight requests to complete. Default is 300 seconds. Set to 35+ seconds for 30-second requests.', distractors: {
      D: 'Health check grace period delays health check evaluation for new instances — not related to deregistration of existing ones.',
      A: 'Sticky sessions route users to the same instance — they do not ensure in-flight requests complete during deregistration.',
      B: 'Slow start gradually increases traffic to new instances — the opposite direction from what is needed here.'
    } },
  { id: 'd2-elb-010', domain: 2, topics: ['ELB', 'HTTPS', 'SSL termination'], type: 'single', stem: 'An ALB terminates HTTPS and forwards HTTP to backend instances. Where are SSL certificates stored?', options: [{ id: 'A', text: 'On each EC2 instance in the /etc/ssl directory' }, { id: 'B', text: 'AWS Certificate Manager (ACM)' }, { id: 'C', text: 'AWS Secrets Manager' }, { id: 'D', text: 'IAM server certificates store' }], correct: ['B'], explanation: 'ALB HTTPS listeners reference certificates from ACM (preferred — free, auto-renews) or IAM server certificates. ACM certificates are attached to ALB listeners by ARN and managed automatically.', distractors: { A: 'Certificates on EC2 would be for end-to-end TLS — not for ALB SSL termination at the load balancer.', C: 'Secrets Manager stores application secrets — not SSL/TLS certificates for ALB.', D: 'IAM server certificates are supported but legacy — ACM is the preferred and recommended approach.' } },
  { id: 'd2-elb-011', domain: 2, topics: ['ELB', 'ALB', 'redirect'], type: 'single', stem: 'All HTTP requests to an ALB should be automatically redirected to HTTPS. How?', options: [{ id: 'A', text: 'Create an ALB listener rule on port 80 with action "Redirect to HTTPS" (port 443, 301)' }, { id: 'B', text: 'Add a NACL rule blocking port 80' }, { id: 'C', text: 'Set the ALB security policy to HTTP-to-HTTPS' }, { id: 'D', text: 'Configure CloudFront in front of the ALB to redirect HTTP to HTTPS' }], correct: ['A'], explanation: 'ALB listener on port 80: add a rule with action "Redirect" to HTTPS (port 443) with HTTP 301. ALB handles the redirect without the traffic reaching backend instances.', distractors: {
      C: 'Security policies control TLS cipher suites — they do not perform HTTP-to-HTTPS redirects.',
      D: 'CloudFront can redirect HTTP to HTTPS but adds cost and complexity — the ALB listener rule is the simpler native solution.',
      B: 'NACL blocking port 80 breaks clients — they receive a connection refused rather than an HTTP redirect.'
    } },
  { id: 'd2-elb-012', domain: 2, topics: ['ELB', 'ALB', 'WAF integration'], type: 'single', stem: 'An ALB needs to block requests with SQL injection patterns in query strings. Integration?', options: [{ id: 'A', text: 'ALB security groups' }, { id: 'B', text: 'Configure an ALB listener rule to block requests containing \'SELECT\' or \'DROP\' keywords' }, { id: 'C', text: 'Enable AWS Shield Advanced on the ALB' }, { id: 'D', text: 'Associate AWS WAF WebACL with the ALB' }], correct: ['D'], explanation: 'AWS WAF WebACL attached to an ALB provides layer 7 protection: managed rule groups for SQLi, XSS, IP reputation, etc. WAF evaluates each HTTP request before it reaches the target group.', distractors: {
      A: 'Security groups filter by IP/port at layer 4 — they cannot inspect HTTP request content for SQL patterns.',
      B: 'ALB listener rules support path, host, and header conditions — not arbitrary string matching for injection patterns.',
      C: 'Shield Advanced provides DDoS protection — it does not inspect request payloads for injection attacks.'
    } },
  { id: 'd2-elb-013', domain: 2, topics: ['ELB', 'access logs'], type: 'single', stem: 'ALB access logs are needed for compliance audit. Where are they stored?', options: [{ id: 'A', text: 'S3 bucket' }, { id: 'B', text: 'Amazon Kinesis Data Firehose' }, { id: 'C', text: 'CloudTrail' }, { id: 'D', text: 'CloudWatch Logs' }], correct: ['A'], explanation: 'ALB access logs are delivered to an S3 bucket in compressed format every 5 minutes. Each log file contains request details: timestamp, client IP, latency, request path, response code, etc.', distractors: {
      D: 'ALB access logs do not go to CloudWatch Logs natively — use CloudWatch Container Insights or query S3 logs with Athena.',
      C: 'CloudTrail records ALB API calls (CreateLoadBalancer, etc.) — not individual request access logs.',
      B: 'ALB does not natively stream to Kinesis Firehose for access logs — Firehose is for CloudWatch Logs subscriptions.'
    } },
  { id: 'd2-elb-014', domain: 2, topics: ['ELB', 'target group', 'slow start'], type: 'single', stem: 'New instances added to a target group receive a huge spike of traffic immediately. How to ramp up gradually?', options: [{ id: 'A', text: 'Deregistration delay' }, { id: 'B', text: 'Slow start mode' }, { id: 'C', text: 'Sticky sessions route existing users away from new instances during warm-up' }, { id: 'D', text: 'Health check grace period' }], correct: ['B'], explanation: 'ALB target group slow start mode: new targets receive gradually increasing traffic over 30–900 seconds. After the ramp period, the target receives its full share. Prevents cold-start overload on new instances.', distractors: {
      C: 'Sticky sessions affect how returning users are routed — they do not control traffic ramp-up for new targets.',
      A: 'Deregistration delay applies to instances being removed — not to newly added targets.',
      D: 'Health check grace period delays health checks on ASG instances — ALB routes traffic as soon as health checks pass, without a slow start.'
    } },
  { id: 'd2-elb-015', domain: 2, topics: ['ELB', 'NLB', 'UDP'], type: 'single', stem: 'An IoT application uses UDP for device telemetry ingestion. Which load balancer supports UDP?', options: [{ id: 'A', text: 'GWLB' }, { id: 'B', text: 'ALB' }, { id: 'C', text: 'NLB' }, { id: 'D', text: 'Classic Load Balancer' }], correct: ['C'], explanation: 'Network Load Balancer supports TCP, UDP, and TLS listeners. It is the correct choice for UDP-based protocols like IoT telemetry, SYSLOG, gaming, VoIP, etc.', distractors: {
      B: 'ALB is HTTP/HTTPS/gRPC only — it does not support UDP or raw TCP.',
      D: 'Classic Load Balancer supports HTTP, HTTPS, and TCP — not UDP.',
      A: 'GWLB uses GENEVE protocol for appliance routing — it is not for IoT UDP ingestion.'
    } },
  { id: 'd2-elb-016', domain: 2, topics: ['ELB', 'ALB', 'authentication'], type: 'single', stem: 'An ALB should authenticate users via Cognito before forwarding to the backend. How?', options: [{ id: 'A', text: 'Configure authentication middleware in each backend instance' }, { id: 'B', text: 'Deploy Lambda@Edge in front of the ALB for Cognito authentication' }, { id: 'C', text: 'Associate a WAF rule that blocks unauthenticated requests to the ALB' }, { id: 'D', text: 'ALB listener rule with authenticate-cognito action before the forward rule' }], correct: ['D'], explanation: 'ALB supports authenticate-cognito and authenticate-oidc listener rule actions. Add an authenticate rule before the forward rule — ALB handles the OAuth2 flow; only authenticated requests reach backends.', distractors: {
      A: 'Per-instance authentication logic adds complexity and inconsistency — ALB centralized auth is the preferred approach.',
      C: 'WAF rules inspect request content — they cannot perform an OAuth2 interactive authentication flow.',
      B: 'Lambda@Edge is for CloudFront, not ALB — ALB has native Cognito authentication built in.'
    } },
  { id: 'd2-elb-017', domain: 2, topics: ['ELB', 'Connection draining', 'graceful shutdown'], type: 'single', stem: 'A deployment terminates instances behind an ALB. Some users see errors during deployment. Root cause?', options: [{ id: 'A', text: 'ALB sticky sessions are preventing routing to new instances' }, { id: 'B', text: 'The deployment is using rolling restart' }, { id: 'C', text: 'Health check interval is too long' }, { id: 'D', text: 'Deregistration delay is set to 0' }], correct: ['D'], explanation: 'Deregistration delay of 0 means instances are immediately removed from the target group with no grace period for in-flight requests to complete — active requests receive 504 or connection reset errors.', distractors: {
      C: 'Long health check intervals would cause delayed detection of new failures — not errors during deregistration.',
      A: 'Sticky sessions would cause failures if the target instance is removed — but the root cause is the deregistration delay setting.',
      B: 'Rolling restarts are a valid deployment strategy — errors occur from the deregistration delay being 0.'
    } },
  { id: 'd2-elb-018', domain: 2, topics: ['ELB', 'ALB', 'weighted target groups'], type: 'single', stem: 'A new application version needs to receive 10% of traffic for canary testing. ALB configuration?', options: [{ id: 'A', text: 'Use Route 53 weighted records' }, { id: 'B', text: 'Use weighted target group routing' }, { id: 'C', text: 'Create two listener rules with different path conditions' }, { id: 'D', text: 'Deploy a Lambda function as a router to forward 10% of requests to the new version' }], correct: ['B'], explanation: 'ALB listener rules support weighted target group routing: forward 90% to v1 target group and 10% to v2. The ALB distributes traffic by weight — no additional infrastructure needed.', distractors: {
      C: 'Different path conditions route by URL — not by traffic percentage split to the same endpoints.',
      A: 'Route 53 weighted routing requires two separate ALBs — ALB weighted target groups achieve this with one ALB.',
      D: 'Lambda proxy adds latency and complexity — ALB weighted routing is the native canary deployment mechanism.'
    } },
  { id: 'd2-elb-019', domain: 2, topics: ['ELB', 'ALB', 'gRPC'], type: 'single', stem: 'A microservices application uses gRPC for inter-service communication. Which load balancer supports gRPC natively?', options: [{ id: 'A', text: 'ALB' }, { id: 'B', text: 'NLB' }, { id: 'C', text: 'GWLB' }, { id: 'D', text: 'Classic Load Balancer' }], correct: ['A'], explanation: 'ALB natively supports gRPC over HTTP/2. It understands gRPC framing and can route by service name and method. Target groups support gRPC health checks. NLB passthrough works but loses L7 visibility.', distractors: {
      B: 'NLB TCP passthrough forwards gRPC traffic but provides no gRPC-aware routing, health checking, or observability.',
      D: 'Classic Load Balancer does not support HTTP/2 or gRPC — it is a legacy product.',
      C: 'GWLB routes to third-party appliances for inspection — it is not a gRPC-aware application load balancer.'
    } },
  { id: 'd2-elb-020', domain: 2, topics: ['ELB', 'ALB', 'Lambda targets'], type: 'single', stem: 'An ALB target group has Lambda functions registered. What is a key limitation?', options: [{ id: 'A', text: 'Lambda request + response body size is limited to 1 MB when routed through ALB' }, { id: 'B', text: 'Lambda targets cannot be in a different account from the ALB' }, { id: 'C', text: 'Lambda targets do not support HTTPS' }, { id: 'D', text: 'Lambda targets only support HTTP GET' }], correct: ['A'], explanation: 'ALB Lambda targets have a 1 MB payload limit for both request and response bodies. For larger payloads, use API Gateway which has a 10 MB limit, or use S3 for body offloading.', distractors: {
      D: 'ALB Lambda targets support all HTTP methods — GET, POST, PUT, DELETE, etc.',
      B: 'Cross-account Lambda targets are supported via resource-based policy on the Lambda function.',
      C: 'SSL termination at ALB is expected for Lambda targets — this is normal behavior, not a limitation.'
    } },
  { id: 'd2-elb-021', domain: 2, topics: ['ELB', 'ALB', 'error codes'], type: 'single', stem: 'A client receives HTTP 502 from an ALB. What does this indicate?', options: [{ id: 'A', text: 'The client request was malformed' }, { id: 'B', text: 'The ALB received an invalid response from a backend target or the connection was refused' }, { id: 'C', text: 'The ALB timed out waiting for a response' }, { id: 'D', text: 'No healthy targets exist in the target group' }], correct: ['B'], explanation: '502 Bad Gateway from ALB: the backend target sent an invalid HTTP response, closed the connection prematurely, or refused the connection. 503 = no healthy targets; 504 = timeout.', distractors: { A: 'Client request issues typically result in 400 Bad Request — 502 is from the ALB receiving a bad backend response.', C: 'ALB timeout returns 504 Gateway Timeout — not 502. 504 means the backend was too slow.', D: 'No healthy targets returns 503 Service Unavailable — not 502. 503 is the "no healthy targets" error code.' } },
  { id: 'd2-elb-022', domain: 2, topics: ['ELB', 'ALB', 'security group'], type: 'single', stem: 'What security group rules are needed for an ALB to accept HTTPS traffic from the internet?', options: [{ id: 'A', text: 'Inbound port 80 and 443 from 0.0.0.0/0 on the ALB security group; outbound to instance SG on listener port' }, { id: 'B', text: 'Inbound port 443 from 0.0.0.0/0 on the ALB security group' }, { id: 'C', text: 'Inbound port 443 from CloudFront IP ranges only' }, { id: 'D', text: 'No security group needed' }], correct: ['B'], explanation: 'ALB security group: inbound port 443 from 0.0.0.0/0 (internet-facing HTTPS). Add port 80 if HTTP redirect is needed. Outbound to instances on the backend port must also be configured.', distractors: {
      A: 'Port 80 is optional if you only want HTTPS — if HTTP redirect is needed, add port 80 also.',
      D: 'ALBs require security groups — they do not automatically accept all internet traffic.',
      C: 'CloudFront IP restriction is a valid architectural choice but not the standard required configuration.'
    } },
  { id: 'd2-elb-023', domain: 2, topics: ['ELB', 'NLB', 'TLS termination'], type: 'single', stem: 'An NLB needs to terminate TLS and forward to backend targets. What is required?', options: [{ id: 'A', text: 'NLB does not support TLS termination' }, { id: 'B', text: 'Enable TLS passthrough on the NLB' }, { id: 'C', text: 'Install an SSL certificate on each backend EC2 instance' }, { id: 'D', text: 'Configure a TLS listener on the NLB with a certificate from ACM or IAM' }], correct: ['D'], explanation: 'NLB TLS listener: configure port 443 with a TLS listener type, attach a certificate from ACM or IAM. NLB terminates TLS and forwards plain traffic (TCP) to backend targets. Supports SNI and multiple certificates.', distractors: {
      C: 'Backend certificates are for end-to-end TLS or mTLS — NLB TLS termination uses its own ACM/IAM certificate.',
      A: 'NLB does support TLS termination — the TLS listener type was added for this purpose.',
      B: 'NLB does not auto-terminate TLS without a TLS listener with certificate configured — passthrough is the default TCP behavior.'
    } },
  { id: 'd2-elb-024', domain: 2, topics: ['ELB', 'ALB', 'headers'], type: 'single', stem: 'Backend EC2 instances behind an ALB need to know the original HTTPS protocol used by the client. Which header?', options: [{ id: 'A', text: 'X-Forwarded-For' }, { id: 'B', text: 'X-Forwarded-Port' }, { id: 'C', text: 'X-Forwarded-Proto' }, { id: 'D', text: 'X-Real-IP' }], correct: ['C'], explanation: 'ALB adds three X-Forwarded headers: X-Forwarded-For (original client IP), X-Forwarded-Proto (http or https), X-Forwarded-Port (original port). X-Forwarded-Proto tells the backend if the client used HTTP or HTTPS.', distractors: {
      D: 'X-Real-IP is an nginx-specific convention — ALB uses X-Forwarded-For for the client IP.',
      A: 'X-Forwarded-For contains the client IP — not the protocol. X-Forwarded-Proto carries the protocol.',
      B: 'X-Forwarded-Port contains the original port — separate from the protocol in X-Forwarded-Proto.'
    } },
  { id: 'd2-elb-025', domain: 2, topics: ['ELB', 'ALB', 'idle timeout'], type: 'single', stem: 'Users report intermittent 504 errors from an ALB during long-running data exports. What to adjust?', options: [{ id: 'A', text: 'Increase the ALB idle timeout (default 60 seconds) to allow long-running backend responses' }, { id: 'B', text: 'Increase the health check interval' }, { id: 'C', text: 'Enable ALB access logging' }, { id: 'D', text: 'Add more instances to the target group' }], correct: ['A'], explanation: 'ALB idle timeout (default 60 seconds): if the backend does not respond within this period, ALB returns 504. Increase the idle timeout beyond the longest expected backend response time for long-running requests.', distractors: { B: 'Health check interval affects failure detection — it does not control the 504 timeout for in-flight requests.', C: 'Access logging provides data but the symptom (long data exports + 504) clearly points to idle timeout.', D: '504 Gateway Timeout indicates timeout, not capacity exhaustion — 503 would indicate no healthy capacity.' } },
  { id: 'd2-elb-026', domain: 2, topics: ['ELB', 'ALB', 'private ALB'], type: 'single', stem: 'An internal ALB is needed that only accepts traffic from within the VPC. What scheme to use?', options: [{ id: 'A', text: 'Internal scheme' }, { id: 'B', text: 'Private scheme with VPC endpoint' }, { id: 'C', text: 'Internet-facing scheme with a security group blocking external traffic' }, { id: 'D', text: 'Public scheme with WAF blocking all traffic from external IP ranges' }], correct: ['A'], explanation: 'Internal (private) ALB: uses only private IP addresses from VPC subnets. No public-facing DNS or IPs assigned. Accessible only from within the VPC or connected networks (VPN, Direct Connect, peering).', distractors: {
      C: 'Internet-facing ALBs have public IPs and public DNS — security groups limit access but the public endpoints still exist.',
      D: 'WAF with IP blocking works but the ALB still has public endpoints — internal scheme eliminates them entirely.',
      B: 'ALBs do not require VPC endpoints for internal access — the internal scheme is the correct configuration.'
    } },
  { id: 'd2-elb-027', domain: 2, topics: ['ELB', 'ALB', 'Rule priority'], type: 'single', stem: 'An ALB has two rules: Rule 1 (priority 100): path /api/* → Target Group A. Rule 2 (priority 200): path /* → Target Group B. A request to /api/users goes to?', options: [{ id: 'A', text: 'Target Group B' }, { id: 'B', text: 'Both' }, { id: 'C', text: 'Target Group B' }, { id: 'D', text: 'Target Group A' }], correct: ['D'], explanation: 'ALB rules are evaluated in ascending priority order (lowest number first). Priority 100 matches /api/* first and routes to TG-A. Priority 200 (default catchall) is never evaluated for this request.', distractors: {
      A: 'Lower priority number = evaluated first in ALB — /* at priority 200 is only evaluated if no earlier rule matches.',
      B: 'ALB applies only the first matching rule — there is no forwarding to multiple target groups from a single request.',
      C: 'Rule priority in ALB is explicit numeric order — there is no implicit specificity-based override.'
    } },
  { id: 'd2-elb-028', domain: 2, topics: ['ELB', 'ALB', 'custom error pages'], type: 'single', stem: 'When the target group returns a 503, a custom error page should be shown instead. ALB solution?', options: [{ id: 'A', text: 'WAF custom block page' }, { id: 'B', text: 'Route 53 health check failover' }, { id: 'C', text: 'CloudFront custom error page' }, { id: 'D', text: 'ALB fixed-response action' }], correct: ['D'], explanation: 'ALB listener rules support a fixed-response action: when a condition matches (or as a fallback), the ALB returns a custom HTTP response (body, status code, content type) without hitting any target.', distractors: {
      A: 'WAF custom responses intercept WAF-blocked requests — not backend 503 errors from target groups.',
      C: 'CloudFront can intercept 503s and show custom pages — valid but adds CloudFront infrastructure when ALB fixed-response is sufficient.',
      B: 'Route 53 health check failover redirects DNS — it cannot intercept individual HTTP 503 responses in real-time.'
    } },
  { id: 'd2-elb-029', domain: 2, topics: ['ELB', 'NLB', 'preserve source IP', 'security groups'], type: 'single', stem: 'Targets behind an NLB need security groups to filter traffic. NLB security group behavior differs from ALB. How?', options: [{ id: 'A', text: 'NLB does not support security groups' }, { id: 'B', text: 'NLB security groups work identically to ALB security groups for all use cases' }, { id: 'C', text: 'NLB supports security groups on the NLB itself but targets see the client IP, not the NLB IP' }, { id: 'D', text: 'NLB uses security groups on the target ENI; target security groups must allow the NLB node IP ranges' }], correct: ['D'], explanation: 'NLB preserves client source IPs. Target security groups must allow either: the client CIDR ranges, or the NLB node private IPs (for Proxy Protocol). Without the correct rules, even healthy targets can block traffic.', distractors: {
      A: 'NLB supports security groups on the NLB itself (added in 2023) — but target SG rules are the key consideration.',
      C: 'Targets do see the client IP (not NLB IP) by default — so target security groups must allow client IP ranges.',
      B: 'NLB and ALB differ significantly — NLB preserves client IP, ALB uses its own node IP as source.'
    } },
  { id: 'd2-elb-030', domain: 2, topics: ['ELB', 'ALB', 'HTTP/2'], type: 'single', stem: 'An ALB front-end uses HTTP/2 from the client. Does the backend need to support HTTP/2?', options: [{ id: 'A', text: 'Yes' }, { id: 'B', text: 'Only if gRPC is used' }, { id: 'C', text: 'Yes' }, { id: 'D', text: 'No' }], correct: ['D'], explanation: 'ALB terminates HTTP/2 on the frontend. Backend connections use HTTP/1.1 by default. For gRPC targets, ALB uses HTTP/2 to the backend. Backends do not need HTTP/2 support for standard web traffic.', distractors: {
      A: 'ALB terminates the frontend protocol — backend connections are a separate HTTP/1.1 pool.',
      C: 'ALB does terminate HTTP/2 — it is not an end-to-end HTTP/2 passthrough for standard web targets.',
      B: 'HTTP/2 passthrough to the backend applies to gRPC and HTTP/2 target groups, not all targets.'
    } },
  { id: 'd2-elb-031', domain: 2, topics: ['ELB', 'health check', 'advanced settings'], type: 'single', stem: 'A health check endpoint takes 3 seconds to respond. Default health check timeout is 5 seconds. Healthy threshold is 3. What is the minimum time for a new instance to be marked healthy?', options: [{ id: 'A', text: '5 seconds' }, { id: 'B', text: '15 seconds' }, { id: 'C', text: 'Default health check interval × healthy threshold (e.g., 30s × 3 = 90 seconds minimum)' }, { id: 'D', text: '3 seconds' }], correct: ['C'], explanation: 'Health check interval (default 30s) × healthy threshold (3) = minimum 90 seconds before an instance is marked healthy. Reducing the interval to 10s with threshold 3 = 30 seconds minimum.', distractors: {
      D: 'One successful health check is not sufficient — the healthy threshold requires consecutive successes.',
      A: 'The timeout is for a single check — minimum healthy time = interval × threshold, not just the timeout.',
      B: 'Timeout × threshold is incorrect — it is interval × threshold. 5-second timeout on a 30-second interval means checks happen every 30 seconds.'
    } },
  { id: 'd2-elb-032', domain: 2, topics: ['ELB', 'ALB', 'mutual TLS'], type: 'single', stem: 'A banking API requires clients to present valid certificates for authentication (mutual TLS). ALB support?', options: [{ id: 'A', text: 'Use NLB for mTLS' }, { id: 'B', text: 'ALB supports mutual TLS (mTLS)' }, { id: 'C', text: 'ALB does not support mutual TLS' }, { id: 'D', text: 'mTLS requires installing a VPN client on each API consumer' }], correct: ['B'], explanation: 'ALB supports mutual TLS: upload a CA certificate bundle as a trust store. Attach it to the HTTPS listener. ALB validates client certificates during TLS handshake. Invalid certs return 400 or are forwarded with verification status.', distractors: {
      C: 'ALB added native mTLS support — API Gateway is not required.',
      A: 'ALB (not just NLB) supports mTLS — NLB passthrough can be used but does not validate certs itself.',
      D: 'Certificate-based client authentication is exactly what mTLS provides — ALB handles this natively.'
    } },
  { id: 'd2-elb-033', domain: 2, topics: ['ELB', 'ALB', 'multi-value headers'], type: 'single', stem: 'A Lambda target function needs all values of a multi-value query parameter. Default ALB behavior passes only the last value. Solution?', options: [{ id: 'A', text: 'Enable multi-value headers on the ALB target group' }, { id: 'B', text: 'Multi-value headers are not supported' }, { id: 'C', text: 'Configure Lambda to parse the raw HTTP request' }, { id: 'D', text: 'Use NLB instead' }], correct: ['A'], explanation: 'ALB Lambda target groups have a multi-value headers option. When enabled, the event object uses multiValueHeaders and multiValueQueryStringParameters — arrays containing all values for repeated keys.', distractors: {
      B: 'Multi-value headers are a supported ALB feature for Lambda targets — no client-side workaround needed.',
      D: 'NLB TCP passthrough is raw bytes — it cannot invoke Lambda; only ALB supports Lambda targets.',
      C: 'Lambda receives only what ALB sends in the JSON event — without multi-value headers enabled, repeated keys are lost.'
    } },
  { id: 'd2-elb-034', domain: 2, topics: ['ELB', 'GWLB', 'deployment'], type: 'single', stem: 'All east-west traffic between VPCs must pass through a centralized third-party NGFW appliance. Solution?', options: [{ id: 'A', text: 'Deploy the NGFW as a NAT instance in each VPC and route through it' }, { id: 'B', text: 'ALB in the inspection VPC with the NGFW as target group' }, { id: 'C', text: 'NLB in front of the NGFW' }, { id: 'D', text: 'Gateway Load Balancer in a centralized inspection VPC' }], correct: ['D'], explanation: 'GWLB is purpose-built for inline network appliance insertion — it distributes traffic to NGFW fleet using GENEVE, maintains source/destination IPs transparently, and scales the appliance fleet automatically.', distractors: {
      A: 'NAT instances in each VPC do not scale — they are per-VPC single points of failure without centralized management.',
      B: 'ALB terminates TCP — the NGFW cannot see original source/destination IPs for policy enforcement.',
      C: 'NLB TCP passthrough does not balance across multiple NGFW instances without GENEVE tunnel support — GWLB is the right product.'
    } },
  { id: 'd2-elb-035', domain: 2, topics: ['ELB', 'ALB', 'query string routing'], type: 'single', stem: 'Mobile clients send ?platform=ios or ?platform=android. Each platform routes to a different target group. ALB rule?', options: [{ id: 'A', text: 'Query string condition in ALB listener rule' }, { id: 'B', text: 'HTTP header condition' }, { id: 'C', text: 'ALB does not support query string routing' }, { id: 'D', text: 'Path-based routing' }], correct: ['A'], explanation: 'ALB listener rules support query string conditions: key=value pairs in the URL query string. IF query string platform=ios THEN forward to iOS target group. Supports exact match and wildcard.', distractors: {
      D: 'Path-based routing requires URL path differences — it cannot inspect query string parameters.',
      B: 'HTTP header conditions work if clients send a header — the question specifies query strings.',
      C: 'ALB natively supports query string routing — API Gateway is not required for this capability.'
    } },
  { id: 'd2-elb-036', domain: 2, topics: ['ELB', 'Connection timeout', 'long polling'], type: 'single', stem: 'An application uses SQS long polling via a backend that holds connections open for 20 seconds. ALB drops connections after 10 seconds. Fix?', options: [{ id: 'A', text: 'Increase the ALB idle timeout to at least 21 seconds (longer than the long poll duration)' }, { id: 'B', text: 'Use WebSocket protocol instead' }, { id: 'C', text: 'Enable connection draining on the ALB to prevent premature connection termination' }, { id: 'D', text: 'Move SQS polling logic to Lambda' }], correct: ['A'], explanation: 'ALB idle timeout controls how long it waits for the backend to respond. Default 60 seconds (mentioned as 10 in this scenario). Increase to 25+ seconds to allow 20-second long poll responses to complete.', distractors: {
      C: 'Connection draining is for graceful deregistration of instances — it does not extend the idle timeout for active connections.',
      B: 'WebSocket connections have the same idle timeout behavior — the idle timeout setting is the root issue.',
      D: 'Changing to Lambda changes the architecture entirely — increasing the ALB timeout is the simpler fix.'
    } },
  { id: 'd2-elb-037', domain: 2, topics: ['ELB', 'ALB', 'IP address type'], type: 'single', stem: 'An ALB should accept traffic from both IPv4 and IPv6 clients. Configuration?', options: [{ id: 'A', text: 'Set the ALB IP address type to dualstack' }, { id: 'B', text: 'Create two ALBs' }, { id: 'C', text: 'Enable IPv6 on the VPC and ALB automatically receives an IPv6 address' }, { id: 'D', text: 'IPv6 is not supported by ALB' }], correct: ['A'], explanation: 'ALB dualstack mode: the load balancer node gets both IPv4 and IPv6 addresses. Clients can connect via either protocol. Backends can be IPv4-only — the ALB performs protocol translation.', distractors: {
      B: 'One ALB with dualstack is sufficient — two ALBs are unnecessary.',
      D: 'ALB natively supports IPv6 with dualstack mode — CloudFront is not required for IPv6 support.',
      C: 'Enabling VPC IPv6 alone does not give the ALB dualstack addresses — the ALB IP address type must be explicitly set to dualstack.'
    } },
  { id: 'd2-elb-038', domain: 2, topics: ['ELB', 'ALB', 'response streaming'], type: 'single', stem: 'A backend sends chunked transfer encoding responses. Does ALB preserve this streaming behavior?', options: [{ id: 'A', text: 'Only if ALB response compression is disabled' }, { id: 'B', text: 'No' }, { id: 'C', text: 'No' }, { id: 'D', text: 'Yes' }], correct: ['D'], explanation: 'ALB supports HTTP/1.1 chunked transfer encoding — it streams response chunks to clients as they arrive from the backend. This enables large responses and server-sent events without full buffering.', distractors: {
      C: 'ALB does not buffer entire responses — it streams chunks as they arrive from the backend.',
      A: 'Response compression is separate from streaming behavior — compression and streaming are independent settings.',
      B: 'ALB does not convert chunked responses to Content-Length — it preserves the chunked encoding.'
    } },
  { id: 'd2-elb-039', domain: 2, topics: ['ELB', 'ALB', 'listener certificate rotation'], type: 'single', stem: 'An ALB listener has an ACM certificate expiring soon. How to update without downtime?', options: [{ id: 'A', text: 'Delete and recreate the ALB listener with the new certificate' }, { id: 'B', text: 'Update the ALB security policy to use the new certificate ARN' }, { id: 'C', text: 'Request a new certificate in ACM and swap the ALB listener certificate in the console' }, { id: 'D', text: 'The certificate auto-renews in ACM' }], correct: ['D'], explanation: 'ACM auto-renews certificates before expiration. ALBs that reference ACM certificates automatically use the renewed certificate — no manual swap or ALB downtime required. This is a key benefit of ACM.', distractors: {
      C: 'Manual certificate swap is needed for non-ACM certificates or if auto-renewal fails — ACM-managed certificates renew and apply automatically.',
      A: 'Recreating the ALB listener causes unnecessary downtime — ACM auto-renewal is seamless.',
      B: 'Security policies control TLS cipher suites — not which certificate is used.'
    } },
  { id: 'd2-elb-040', domain: 2, topics: ['ELB', 'NLB', 'performance'], type: 'single', stem: 'An application has 1M requests per second spikes. Which ELB type handles extreme traffic without pre-warming?', options: [{ id: 'A', text: 'NLB' }, { id: 'B', text: 'GWLB' }, { id: 'C', text: 'ALB' }, { id: 'D', text: 'Classic Load Balancer' }], correct: ['A'], explanation: 'NLB handles millions of requests per second with sub-millisecond latency. It scales instantly — no pre-warming needed. ALB is also auto-scaling but handles fewer RPS and may benefit from pre-warming at extreme loads.', distractors: {
      C: 'ALB is powerful but NLB is the choice for maximum throughput — ALB adds more processing overhead for HTTP parsing.',
      D: 'Classic Load Balancer is legacy and has lower throughput limits — it is not recommended for new workloads.',
      B: 'GWLB is for appliance routing — not for general high-throughput application serving.'
    } },
  { id: 'd2-elb-041', domain: 2, topics: ['ELB', 'ALB', 'target group monitoring'], type: 'single', stem: 'Which CloudWatch metric indicates that an ALB target group has unhealthy instances?', options: [{ id: 'A', text: 'RequestCountPerTarget' }, { id: 'B', text: 'TargetResponseTime' }, { id: 'C', text: 'HealthyHostCount' }, { id: 'D', text: 'UnHealthyHostCount' }], correct: ['D'], explanation: 'UnHealthyHostCount metric on the target group: the number of targets failing health checks at any given time. Set a CloudWatch alarm when this is > 0 to alert when any instance becomes unhealthy.', distractors: {
      C: 'HealthyHostCount shows the number of passing instances — not the number of failing ones.',
      A: 'RequestCountPerTarget shows traffic distribution — not health status.',
      B: 'TargetResponseTime shows latency — not directly whether targets are passing or failing health checks.'
    } },
  { id: 'd2-elb-042', domain: 2, topics: ['ELB', 'GWLB', 'endpoint'], type: 'single', stem: 'A GWLB is deployed in a centralized inspection VPC. How do spoke VPCs route traffic through the GWLB?', options: [{ id: 'A', text: 'Create GWLB endpoints in the spoke VPCs' }, { id: 'B', text: 'Both B and C' }, { id: 'C', text: 'Transit Gateway connects spoke VPCs to the inspection VPC for GWLB access' }, { id: 'D', text: 'Create a VPC peering connection to the inspection VPC and update route tables' }], correct: ['A'], explanation: 'GWLB Endpoints (GWLBe) are Interface Endpoints in each spoke VPC. Route tables point to the GWLBe as a next-hop. Traffic flows through GENEVE tunnels to the GWLB fleet for inspection, then returned.', distractors: {
      D: 'VPC peering alone does not route traffic through the GWLB — GWLB endpoints are the specific mechanism.',
      C: 'TGW connects VPCs for routing — GWLB endpoints are the mechanism for traffic inspection insertion.',
      B: 'GWLB endpoints alone are sufficient — TGW is not required for the GWLB inspection architecture.'
    } },
  { id: 'd2-elb-043', domain: 2, topics: ['ELB', 'ALB', 'request tracing'], type: 'single', stem: 'ALB access logs need to correlate with application logs. Which ALB feature provides a unique ID per request?', options: [{ id: 'A', text: 'X-Forwarded-For header' }, { id: 'B', text: 'ALB request ID in the access log entry' }, { id: 'C', text: 'X-Amzn-Trace-Id header' }, { id: 'D', text: 'AWS X-Ray' }], correct: ['C'], explanation: 'ALB injects X-Amzn-Trace-Id into each request. This trace ID appears in ALB access logs. Backend applications can log this header to correlate ALB logs with application logs. Also integrates with AWS X-Ray.', distractors: {
      A: 'X-Forwarded-For contains the client IP — not a trace ID. X-Amzn-Trace-Id is the trace ID header.',
      B: 'The ALB access log has a trace_id field — but the X-Amzn-Trace-Id header is what backends log for correlation.',
      D: 'X-Ray uses the X-Amzn-Trace-Id header — it provides distributed tracing built on top of this same trace ID.'
    } },
  { id: 'd2-elb-044', domain: 2, topics: ['ELB', 'ALB', 'source IP preservation'], type: 'single', stem: 'Backend EC2 instances see ALB node IP instead of the client IP. Which header provides the real client IP?', options: [{ id: 'A', text: 'X-Forwarded-For' }, { id: 'B', text: 'X-Forwarded-Port' }, { id: 'C', text: 'X-Forwarded-Proto' }, { id: 'D', text: 'X-Real-IP' }], correct: ['A'], explanation: 'ALB adds X-Forwarded-For with the original client IP. Backend sees the ALB node IP as source (for return routing), but X-Forwarded-For contains the real client IP for logging, access control, and geolocation.', distractors: {
      C: 'X-Forwarded-Proto contains the original protocol (http/https) — not the client IP address.',
      D: 'ALB does not set X-Real-IP — it uses the standard X-Forwarded-For header for client IP.',
      B: 'X-Forwarded-Port contains the original port — not the IP address or a combined IP+port.'
    } },
  { id: 'd2-elb-045', domain: 2, topics: ['ELB', 'ALB', 'NLB', 'selection'], type: 'multi', stem: 'Which TWO criteria would lead you to choose NLB over ALB?', options: [{ id: 'A', text: 'The application uses TCP on a non-HTTP port (e.g., MQTT on port 1883 for IoT)' }, { id: 'B', text: 'Users must be authenticated via Cognito before accessing the application' }, { id: 'C', text: 'The application requires path-based routing between microservices' }, { id: 'D', text: 'Clients need to whitelist a static IP address for firewall rules' }], correct: ['A', 'D'], explanation: 'NLB is preferred for: (1) non-HTTP protocols (TCP, UDP, TLS) — ALB is HTTP/HTTPS only; (2) static IP requirements — NLB supports Elastic IPs. ALB handles path-based routing and Cognito authentication.', distractors: {
      C: 'Path-based routing is an ALB feature — NLB operates at Layer 4 without HTTP content inspection.',
      B: 'Cognito authentication is an ALB listener rule feature — NLB has no application-layer authentication capability.'
    } },
]
