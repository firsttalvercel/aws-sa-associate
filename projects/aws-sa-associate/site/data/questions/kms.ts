import type { Question } from '../../lib/types'
export const kms_questions: Question[] = [
  { id: 'd1-kms-001', domain: 1, topics: ['KMS', 'CMK', 'key types'], type: 'single', stem: 'What is the difference between an AWS managed key and a customer managed key in KMS?', options: [{ id: 'A', text: 'Customer managed keys allow custom key policies and rotation control; AWS managed keys are controlled entirely by AWS' }, { id: 'B', text: 'AWS managed keys can be deleted by customers; customer managed keys cannot' }, { id: 'C', text: 'Customer managed keys are stored outside AWS on customer HSMs by default' }, { id: 'D', text: 'AWS managed keys have higher encryption strength than customer managed keys' }], correct: ['A'], explanation: 'Customer managed keys let you control the key policy, enable/disable, rotate, and schedule deletion. AWS managed keys are created and managed by AWS on your behalf for each service.', distractors: { A: 'AWS managed keys cannot be deleted by customers — only customer managed keys can be scheduled for deletion.', C: 'Both types use AES-256 encryption — no difference in cryptographic strength.', D: 'Customer managed keys are stored in AWS HSMs (FIPS 140-2 Level 3) by default, not on customer hardware.' } },
  { id: 'd1-kms-002', domain: 1, topics: ['KMS', 'key policy', 'access control'], type: 'single', stem: 'A KMS key policy does not include the AWS account root principal. What happens to IAM-based access?', options: [{ id: 'A', text: 'IAM policies alone can still grant KMS access when key policy is silent' }, { id: 'B', text: 'The key becomes inaccessible and must be recovered by AWS Support' }, { id: 'C', text: 'KMS automatically adds the root principal to prevent key lockout within 24 hours' }, { id: 'D', text: 'IAM policies cannot grant KMS access unless the key policy explicitly allows it for the account' }], correct: ['D'], explanation: 'KMS key policies are the primary access control. If the key policy does not include the account root or IAM delegation statement, IAM policies alone cannot grant access — the key policy controls who can use the key.', distractors: { A: 'Unlike most AWS services, IAM alone cannot grant KMS key access without key policy support.', C: 'The key is not inaccessible — it just means only principals explicitly in the key policy can use it.', D: 'KMS does not auto-modify key policies — this is why careful key policy design is critical.' } },
  { id: 'd1-kms-003', domain: 1, topics: ['KMS', 'envelope encryption'], type: 'single', stem: 'Why does S3 use envelope encryption with KMS rather than encrypting each object directly with the KMS key?', options: [{ id: 'A', text: 'Envelope encryption reduces KMS API calls and cost' }, { id: 'B', text: 'S3 does not support direct KMS API calls due to network latency limitations' }, { id: 'C', text: 'KMS keys expire every 24 hours making direct encryption impractical for storage' }, { id: 'D', text: 'KMS keys cannot encrypt objects larger than 4 KB directly' }], correct: ['D'], explanation: 'KMS can only encrypt data up to 4 KB directly. For larger data, KMS generates a data key (plaintext + encrypted copy). Data is encrypted locally with the plaintext data key; only the small encrypted data key is stored.', distractors: { B: 'While cost reduction is a benefit, the primary reason is the 4 KB limit on direct KMS encryption.', C: 'S3 does call KMS APIs directly — for generating data keys, not for encrypting objects directly.', D: 'KMS keys do not expire — automatic rotation is optional and occurs annually, not daily.' } },
  { id: 'd1-kms-004', domain: 1, topics: ['KMS', 'automatic rotation'], type: 'single', stem: 'Automatic key rotation is enabled on a KMS customer managed key. What happens to data encrypted with the old key material?', options: [{ id: 'A', text: 'Old encrypted data must be re-encrypted manually after rotation' }, { id: 'B', text: 'KMS retains old key material; existing ciphertext remains decryptable without re-encryption' }, { id: 'C', text: 'Old encrypted data becomes permanently inaccessible after rotation' }, { id: 'D', text: 'Rotation only applies to new data; old key material is immediately purged' }], correct: ['B'], explanation: 'KMS retains all previous key material versions. Old ciphertext can be decrypted using the version of the key material that encrypted it. Only new encryption operations use the newest material.', distractors: { A: 'Re-encryption is not required — KMS handles decryption with the correct key material version automatically.', C: 'Old data remains accessible — previous key material versions are retained, not destroyed.', D: 'Rotation does not purge old material — all versions are retained for decryption.' } },
  { id: 'd1-kms-005', domain: 1, topics: ['KMS', 'grants'], type: 'single', stem: 'What is a KMS grant and when is it preferred over a key policy update?', options: [{ id: 'A', text: 'A grant is required for AWS managed keys since their key policies cannot be edited' }, { id: 'B', text: 'A grant is a permanent key policy addition useful for long-term cross-account access' }, { id: 'C', text: 'A grant delegates key usage to a principal programmatically without modifying the key policy' }, { id: 'D', text: 'A grant is an IAM policy attached to a user that references a KMS key ARN' }], correct: ['C'], explanation: 'Grants are programmatic delegations of KMS permissions that can be created and retired without touching the key policy. Services like AWS Secrets Manager use grants for temporary per-secret encryption permissions.', distractors: { A: 'Grants can be retired programmatically — they are preferred for temporary or dynamic access, not permanent.', C: 'Grants are KMS-native constructs — not IAM policies. They reference the key and a grantee principal.', D: 'Grants work with customer managed keys; AWS managed keys already grant access through service-specific policies.' } },
  { id: 'd1-kms-006', domain: 1, topics: ['KMS', 'multi-region keys'], type: 'single', stem: 'A company needs to encrypt DynamoDB data in us-east-1 and decrypt it in eu-west-1 without re-encrypting. What KMS feature supports this?', options: [{ id: 'A', text: 'Enable cross-region replication on the KMS key with a replication factor of 2' }, { id: 'B', text: 'Use AWS managed keys' }, { id: 'C', text: 'Copy the customer managed key from us-east-1 to eu-west-1 using key export' }, { id: 'D', text: 'Use KMS multi-region keys' }], correct: ['D'], explanation: 'Multi-region keys have the same key ID and material in multiple regions. Ciphertext encrypted in one region can be decrypted in another without a decrypt-then-re-encrypt step.', distractors: { A: 'KMS key material cannot be exported from AWS HSMs for security reasons.', C: 'KMS keys do not have a replication factor setting — multi-region keys are a specific key type.', D: 'AWS managed keys are region-specific and not shared across regions.' } },
  { id: 'd1-kms-007', domain: 1, topics: ['KMS', 'CloudTrail', 'audit'], type: 'single', stem: 'How can you determine which IAM principal called KMS Decrypt on a key yesterday?', options: [{ id: 'A', text: 'Use KMS key usage metrics in CloudWatch for per-principal breakdowns' }, { id: 'B', text: 'Review the KMS console key usage tab for a 7-day caller history' }, { id: 'C', text: 'Check CloudTrail management events for kms:Decrypt API calls with caller identity' }, { id: 'D', text: 'Review the KMS key policy' }], correct: ['C'], explanation: 'All KMS API calls — including Decrypt, Encrypt, GenerateDataKey — are logged as management events in CloudTrail with the caller\'s identity.', distractors: { A: 'Key policies define access control — they do not log API calls or audit access.', C: 'CloudWatch KMS metrics show call counts and error rates — not per-principal identity breakdown.', D: 'The KMS console does not provide a caller history tab — CloudTrail is the audit source.' } },
  { id: 'd1-kms-008', domain: 1, topics: ['KMS', 'key deletion'], type: 'single', stem: 'A developer schedules a KMS customer managed key for deletion. What is the minimum waiting period?', options: [{ id: 'A', text: '24 hours' }, { id: 'B', text: '7 days minimum' }, { id: 'C', text: '90 days' }, { id: 'D', text: 'Immediate deletion if no grants are attached to the key at time of request' }], correct: ['B'], explanation: 'KMS scheduled deletion waiting period: 7–30 days (configurable). During this window deletion can be cancelled. Deleted keys leave ciphertext permanently inaccessible — this window is intentional.', distractors: { A: '24 hours is not the minimum — 7 days is the minimum configurable waiting period.', C: '90 days has no basis in AWS KMS deletion policy — the maximum is 30 days.', D: 'Grants do not affect the deletion waiting period requirement — all deletions have a minimum wait.' } },
  { id: 'd1-kms-009', domain: 1, topics: ['KMS', 'S3', 'SSE-KMS vs SSE-S3'], type: 'single', stem: 'What is the main difference between S3 SSE-KMS and SSE-S3?', options: [{ id: 'A', text: 'SSE-KMS uses customer-controlled or AWS managed KMS keys with CloudTrail audit; SSE-S3 uses AWS-managed keys with no customer key control or per-object audit logging' }, { id: 'B', text: 'SSE-KMS requires HTTPS; SSE-S3 works with both HTTP and HTTPS connections' }, { id: 'C', text: 'SSE-S3 uses AES-256; SSE-KMS uses AES-128 with additional key wrapping' }, { id: 'D', text: 'SSE-S3 encrypts the entire bucket; SSE-KMS encrypts individual objects only' }], correct: ['A'], explanation: 'SSE-KMS provides CloudTrail audit of every Decrypt call and lets you use customer managed keys. SSE-S3 uses AWS-managed keys automatically with no key control or per-call audit logging.', distractors: { A: 'Both SSE-KMS and SSE-S3 use AES-256 — the difference is in key management, not algorithm.', C: 'Both encryption types operate at the object level — neither encrypts the bucket itself as a unit.', D: 'Both require HTTPS in transit regardless of server-side encryption type.' } },
  { id: 'd1-kms-010', domain: 1, topics: ['KMS', 'ViaService', 'key policy condition'], type: 'single', stem: 'A key policy uses kms:ViaService condition. What does this restrict?', options: [{ id: 'A', text: 'Restricts key use to requests that originate from a specific AWS service (e.g., s3.amazonaws.com)' }, { id: 'B', text: 'Requires that KMS requests originate from a specific VPC endpoint' }, { id: 'C', text: 'Limits which IAM roles in the account can call KMS GenerateDataKey' }, { id: 'D', text: 'Limits which AWS regions can use the KMS key for encryption operations' }], correct: ['A'], explanation: 'kms:ViaService restricts key use to requests made through a specific AWS service. For example, only allow Decrypt when the request comes from secretsmanager.amazonaws.com.', distractors: { A: 'Region restrictions are not a function of kms:ViaService — it identifies the calling service, not region.', C: 'IAM role restrictions are handled by key policy Principal and IAM conditions — not kms:ViaService.', D: 'VPC endpoint restrictions use kms:CallerAccount and aws:SourceVpce — not kms:ViaService.' } },
  { id: 'd1-kms-011', domain: 1, topics: ['KMS', 'asymmetric keys'], type: 'single', stem: 'When would you use a KMS asymmetric key instead of a symmetric key?', options: [{ id: 'A', text: 'Asymmetric keys are only available in the AWS GovCloud regions for compliance' }, { id: 'B', text: 'When the recipient needs to decrypt data without having AWS credentials' }, { id: 'C', text: 'When encrypting data larger than 4 KB to avoid the KMS direct encryption limit' }, { id: 'D', text: 'When you need cross-region encryption' }], correct: ['B'], explanation: 'Asymmetric KMS keys (RSA, ECC) generate a public/private key pair. The public key can be downloaded and used outside AWS for encryption or signature verification, enabling offline or cross-system cryptographic operations.', distractors: { A: 'The 4 KB limit applies to direct KMS encryption regardless of key type — use envelope encryption for large data.', C: 'Asymmetric keys are available in all commercial regions — not restricted to GovCloud.', D: 'Multi-region keys handle cross-region replication — asymmetric keys do not replicate automatically.' } },
  { id: 'd1-kms-012', domain: 1, topics: ['KMS', 'cross-account', 'key sharing'], type: 'single', stem: 'Account A wants Account B to encrypt data using Account A\'s KMS key. What is required?', options: [{ id: 'A', text: 'Create a KMS key alias in Account B pointing to the Account A key ARN' }, { id: 'B', text: 'Copy the KMS key to Account B using the key export feature' }, { id: 'C', text: 'Update Account A\'s key policy to allow Account B; create IAM policy in Account B for the principal' }, { id: 'D', text: 'Enable cross-account KMS sharing via AWS Organizations resource sharing' }], correct: ['C'], explanation: 'Cross-account KMS access requires two steps: (1) key policy in Account A allows Account B principal, (2) IAM policy in Account B allows the action on the Account A key ARN.', distractors: { A: 'KMS keys cannot be exported or copied between accounts — key material stays in AWS HSMs.', C: 'AWS Resource Access Manager does not support KMS key sharing — key policies are used instead.', D: 'Aliases are scoped to a single account — they cannot reference keys in other accounts.' } },
  { id: 'd1-kms-013', domain: 1, topics: ['KMS', 'data key caching'], type: 'single', stem: 'An application calls KMS GenerateDataKey thousands of times per second. How can this be optimized?', options: [{ id: 'A', text: 'Increase the KMS key quota by requesting a limit increase via Service Quotas' }, { id: 'B', text: 'Deploy a local HSM to handle key generation and reduce KMS API dependency' }, { id: 'C', text: 'Switch to AWS managed keys which have higher API rate limits than customer managed keys' }, { id: 'D', text: 'Use the AWS Encryption SDK data key caching' }], correct: ['D'], explanation: 'The AWS Encryption SDK\'s data key caching reuses data keys (within a configured message count or time limit) to reduce KMS API calls and latency. A common pattern for high-throughput encryption workloads.', distractors: { A: 'KMS has default request quotas (e.g., 5,500/sec for GenerateDataKey) — service quota increases help but caching is more cost-effective.', C: 'AWS managed keys have the same rate limits as customer managed keys.', D: 'Local HSMs require hardware management and do not integrate with KMS envelope encryption patterns.' } },
  { id: 'd1-kms-014', domain: 1, topics: ['KMS', 'key usage', 'encrypt decrypt'], type: 'single', stem: 'Which KMS API operation should an application use to encrypt data under 4 KB directly?', options: [{ id: 'A', text: 'kms:GenerateDataKeyWithoutPlaintext for in-memory encryption operations' }, { id: 'B', text: 'kms:ReEncrypt to convert existing plaintext into KMS-encrypted ciphertext' }, { id: 'C', text: 'kms:Encrypt to encrypt the data directly using the CMK' }, { id: 'D', text: 'kms:GenerateDataKey to get a plaintext data key for local encryption' }], correct: ['C'], explanation: 'kms:Encrypt directly encrypts up to 4 KB of data using the CMK and returns the ciphertext. Use kms:Decrypt to reverse it.', distractors: { A: 'GenerateDataKey returns a key for encrypting larger data locally — not for direct CMK encryption.', C: 'GenerateDataKeyWithoutPlaintext returns an encrypted data key for deferred decryption workflows — not direct encryption.', D: 'ReEncrypt re-encrypts existing KMS ciphertext under a different key — not plaintext-to-ciphertext.' } },
  { id: 'd1-kms-015', domain: 1, topics: ['KMS', 'BYOK', 'key import'], type: 'single', stem: 'A company wants to import their own key material into KMS. What is a critical limitation?', options: [{ id: 'A', text: 'Imported keys are stored in the company\'s on-premises HSM and called remotely by KMS' }, { id: 'B', text: 'Imported key material cannot use automatic rotation' }, { id: 'C', text: 'Imported keys cannot be used for S3 server-side encryption under any circumstances' }, { id: 'D', text: 'Key import requires AWS GovCloud and is not available in commercial regions' }], correct: ['B'], explanation: 'Keys with imported material do not support automatic annual rotation. To rotate, you must import new material into a new CMK and update all references. You also retain the original key material backup.', distractors: { A: 'Imported keys work with S3 SSE-KMS, EBS, RDS, and other services like any customer managed key.', C: 'Imported key material is stored in AWS KMS HSMs after import — not on customer HSMs.', D: 'Key import is available in all AWS regions including commercial — no GovCloud restriction.' } },
  { id: 'd1-kms-016', domain: 1, topics: ['KMS', 'EBS', 'encryption'], type: 'single', stem: 'An EBS volume is encrypted with KMS. The EC2 instance terminates and the volume is detached. Can the data be recovered?', options: [{ id: 'A', text: 'Yes' }, { id: 'B', text: 'No' }, { id: 'C', text: 'Yes' }, { id: 'D', text: 'No' }], correct: ['A'], explanation: 'EBS encryption is tied to the KMS key, not the EC2 instance. The encrypted volume persists after detachment. Any instance with KMS key access can reattach and read the data.', distractors: { A: 'EBS encryption does not tie the key to an instance — the key is a KMS CMK, not instance-specific.', C: 'EBS volumes are not wiped on detachment — the encrypted data remains intact on the volume.', D: 'Encrypted EBS volumes can be attached to instances in the same AZ; no cross-AZ attachment without snapshot.' } },
  { id: 'd1-kms-017', domain: 1, topics: ['KMS', 'key policy', 'root'], type: 'single', stem: 'What must a KMS key policy include to allow IAM policies to control access to the key?', options: [{ id: 'A', text: 'A statement explicitly granting every IAM role that needs access individually' }, { id: 'B', text: 'A condition requiring aws:SourceAccount matching for all key operations' }, { id: 'C', text: 'A Principal:* Allow statement to delegate all key access management to IAM' }, { id: 'D', text: 'A statement giving the AWS account (root principal) permission to enable IAM policy control' }], correct: ['D'], explanation: 'The standard KMS key policy includes a statement like "Principal: arn:aws:iam::ACCOUNT:root" — this enables IAM policies in the account to control access. Without it, only principals explicitly in the key policy can use the key.', distractors: { A: 'Listing individual roles is an option but the root account statement is what enables IAM delegation.', C: 'aws:SourceAccount is a condition key for cross-service delegation — not for enabling IAM delegation.', D: 'Principal:* is overly broad and grants access to anonymous users — never use it in key policies.' } },
  { id: 'd1-kms-018', domain: 1, topics: ['KMS', 'SSE-C', 'customer keys'], type: 'single', stem: 'S3 SSE-C allows customers to supply their own encryption keys. What responsibility does this place on the customer?', options: [{ id: 'A', text: 'Customer must supply the same key with every GET request' }, { id: 'B', text: 'SSE-C requires CloudHSM to generate and manage the keys provided to S3' }, { id: 'C', text: 'Customer keys are stored in Secrets Manager and referenced by ARN in S3 requests' }, { id: 'D', text: 'The customer only needs to provide the key at upload time; S3 stores it securely for later retrieval' }], correct: ['A'], explanation: 'SSE-C: S3 uses the key to encrypt/decrypt but never stores it. Customers must supply the exact same key on every GET. No key = no access permanently. Very high operational burden.', distractors: { A: 'S3 explicitly does not store SSE-C keys — supplying it at upload is not sufficient for later retrieval.', C: 'SSE-C supports any customer-managed key source — CloudHSM is not required.', D: 'SSE-C keys are sent in HTTP headers per-request — they are not ARN-referenced from Secrets Manager.' } },
  { id: 'd1-kms-019', domain: 1, topics: ['KMS', 'RDS', 'encryption'], type: 'single', stem: 'An unencrypted RDS database needs to be encrypted. What is the process?', options: [{ id: 'A', text: 'Enable encryption on the existing RDS instance via the modify instance settings' }, { id: 'B', text: 'Add an encrypted EBS volume to the existing instance to store encrypted data' }, { id: 'C', text: 'Take a snapshot, copy the snapshot with encryption enabled, restore a new encrypted instance' }, { id: 'D', text: 'Enable AWS Macie on the RDS instance to automatically encrypt data at rest' }], correct: ['C'], explanation: 'RDS encryption must be enabled at creation. To encrypt an existing unencrypted instance: snapshot → copy snapshot with encryption enabled → restore encrypted instance from the encrypted snapshot.', distractors: { A: 'You cannot modify encryption on a running RDS instance — it must be enabled at creation or via snapshot restore.', C: 'RDS manages its own storage — you cannot add separate encrypted EBS volumes to an RDS instance.', D: 'Amazon Macie is a data classification service for S3 — it does not encrypt RDS databases.' } },
  { id: 'd1-kms-020', domain: 1, topics: ['KMS', 'key policy', 'deny'], type: 'single', stem: 'A key policy has an explicit Deny for kms:Decrypt to a specific IAM role. An IAM policy allows kms:Decrypt for that role. Result?', options: [{ id: 'A', text: 'Allow' }, { id: 'B', text: 'Allow' }, { id: 'C', text: 'Allow' }, { id: 'D', text: 'Deny' }], correct: ['D'], explanation: 'KMS evaluation follows standard IAM rules: explicit deny wins. A key policy deny overrides any IAM allow for the same action.', distractors: { A: 'No override exists for IAM policies over key policy explicit denies — deny always wins.', C: 'No policy type takes precedence over explicit deny — the evaluation is not ordered by type.', D: 'Key policy denies apply to all principals — both same-account and cross-account.' } },
  { id: 'd1-kms-021', domain: 1, topics: ['KMS', 'CloudHSM', 'difference'], type: 'single', stem: 'When should CloudHSM be used instead of KMS for key management?', options: [{ id: 'A', text: 'When you need lower latency than KMS API calls for encryption operations' }, { id: 'B', text: 'When you need encryption for S3 and DynamoDB' }, { id: 'C', text: 'When compliance requires single-tenant HSM hardware with sole control of key material' }, { id: 'D', text: 'When you need automatic annual key rotation without manual intervention' }], correct: ['C'], explanation: 'CloudHSM provides single-tenant, dedicated HSMs where only the customer has access to key material. Required for some compliance frameworks (PCI, FIPS 140-2 Level 3 with sole control). KMS HSMs are shared (logical isolation only).', distractors: { A: 'KMS supports automatic rotation — no reason to use CloudHSM specifically for rotation convenience.', C: 'CloudHSM can have lower latency for specific operations but that is not the primary differentiation.', D: 'KMS integrates natively with S3 and DynamoDB — CloudHSM does not have native S3/DynamoDB integration.' } },
  { id: 'd1-kms-022', domain: 1, topics: ['KMS', 'GenerateDataKeyPair'], type: 'single', stem: 'What does kms:GenerateDataKeyPair return?', options: [{ id: 'A', text: 'A symmetric data key in plaintext and encrypted form for envelope encryption' }, { id: 'B', text: 'A public key and a private key encrypted under the CMK for asymmetric envelope encryption' }, { id: 'C', text: 'Two identical data keys' }, { id: 'D', text: 'A public/private RSA pair stored externally on the customer\'s key management system' }], correct: ['B'], explanation: 'GenerateDataKeyPair returns: (1) plaintext public key, (2) plaintext private key, (3) private key encrypted under the CMK. Use for RSA or ECC asymmetric encryption workflows outside KMS.', distractors: { A: 'GenerateDataKey (without Pair) returns a symmetric key in both plaintext and encrypted form.', C: 'There is no "dual-key" API — asymmetric operations use a public/private pair, not two identical keys.', D: 'Keys are generated and partially returned in AWS — the encrypted private key stays in AWS until decrypted.' } },
  { id: 'd1-kms-023', domain: 1, topics: ['KMS', 'tags', 'cost allocation'], type: 'single', stem: 'A company wants to allocate KMS costs per application team. What should be applied to KMS keys?', options: [{ id: 'A', text: 'Apply cost allocation tags to KMS keys' }, { id: 'B', text: 'Use separate AWS accounts per team to isolate KMS billing entirely' }, { id: 'C', text: 'Create separate KMS key aliases per team' }, { id: 'D', text: 'KMS costs are not attributable to individual keys in Cost Explorer' }], correct: ['A'], explanation: 'KMS keys support cost allocation tags. Apply tags (e.g., Team=payments), activate them in Billing, and Cost Explorer will show KMS costs per tag value.', distractors: { A: 'Key aliases help with key identification — they are not used as cost allocation dimensions in billing.', C: 'KMS key costs can be attributed via cost allocation tags — they do appear in Cost Explorer.', D: 'Separate accounts work but are operationally heavy — cost allocation tags are the lightweight solution.' } },
  { id: 'd1-kms-024', domain: 1, topics: ['KMS', 'Secrets Manager', 'integration'], type: 'single', stem: 'Secrets Manager uses KMS to encrypt secrets. What KMS permission must the Secrets Manager service have?', options: [{ id: 'A', text: 'kms:Encrypt and kms:Decrypt on the key to read and write secret values' }, { id: 'B', text: 'kms:CreateKey to generate a new KMS key automatically for each secret' }, { id: 'C', text: 'kms:DescribeKey only' }, { id: 'D', text: 'kms:GenerateDataKey and kms:Decrypt on the key' }], correct: ['D'], explanation: 'Secrets Manager uses envelope encryption: kms:GenerateDataKey to get a data key when storing a secret, kms:Decrypt to decrypt the data key when retrieving. The key policy must allow secretsmanager.amazonaws.com these actions.', distractors: { A: 'Secrets Manager does not call kms:Encrypt directly — it uses GenerateDataKey for envelope encryption.', C: 'Secrets Manager uses an existing KMS key — it does not create keys automatically.', D: 'Secrets Manager does use the KMS API — it does not have built-in independent encryption.' } },
  { id: 'd1-kms-025', domain: 1, topics: ['KMS', 'key spec', 'SYMMETRIC_DEFAULT'], type: 'single', stem: 'What algorithm does a KMS key with SYMMETRIC_DEFAULT key spec use?', options: [{ id: 'A', text: 'AES-256-GCM for authenticated encryption of data and data keys' }, { id: 'B', text: 'ChaCha20-Poly1305 for high-performance authenticated encryption' }, { id: 'C', text: 'Triple-DES (3DES) for legacy compatibility with older applications' }, { id: 'D', text: 'RSA 2048 for both encryption and signing operations' }], correct: ['A'], explanation: 'KMS SYMMETRIC_DEFAULT keys use AES-256-GCM — 256-bit AES in Galois/Counter Mode providing authenticated encryption. This is the standard KMS symmetric key used for most service integrations.', distractors: { A: 'RSA 2048 is used by KMS asymmetric keys — not the SYMMETRIC_DEFAULT spec.', C: 'KMS does not support 3DES — AES-256 is the symmetric standard.', D: 'KMS does not offer ChaCha20 — AES-256-GCM is the algorithm for symmetric keys.' } },
  { id: 'd1-kms-026', domain: 1, topics: ['KMS', 'key disable'], type: 'single', stem: 'A KMS key is disabled. What happens to data encrypted with it?', options: [{ id: 'A', text: 'All ciphertext is immediately decrypted and stored in plaintext for continuity' }, { id: 'B', text: 'Existing decryption continues to work; only new encryption operations are blocked' }, { id: 'C', text: 'Encryption and decryption fail while disabled; data remains encrypted and inaccessible until key is re-enabled' }, { id: 'D', text: 'Disabling a key triggers automatic re-encryption under a backup key KMS creates' }], correct: ['C'], explanation: 'Disabling a KMS key prevents all cryptographic operations. Existing ciphertext cannot be decrypted until the key is re-enabled. Disabling is reversible — deletion is permanent.', distractors: { A: 'KMS does not decrypt data when a key is disabled — data remains encrypted but inaccessible.', C: 'Disabling blocks both encryption and decryption — not just new encryption.', D: 'KMS does not create backup keys or automatically re-encrypt data on key disable.' } },
  { id: 'd1-kms-027', domain: 1, topics: ['KMS', 'S3', 'bucket key'], type: 'single', stem: 'What does enabling S3 Bucket Key for SSE-KMS do?', options: [{ id: 'A', text: 'Generates a short-lived bucket-level data key to reduce KMS API calls per object' }, { id: 'B', text: 'Combines SSE-KMS and SSE-S3 encryption in a two-layer scheme for each object' }, { id: 'C', text: 'Creates a separate KMS CMK per bucket automatically to improve isolation' }, { id: 'D', text: 'Enables per-object KMS keys instead of one key for the entire bucket' }], correct: ['A'], explanation: 'S3 Bucket Key: KMS generates a bucket-level data key cached in S3 for a short time. Individual object encryptions use this cached key, reducing KMS API calls by up to 99% — significant cost and latency savings for high-object-count buckets.', distractors: { A: 'Bucket Key uses the existing CMK — it does not create new CMKs per bucket.', C: 'Bucket Key reduces KMS calls by sharing a key across objects — opposite of per-object keys.', D: 'Bucket Key is a performance optimization within SSE-KMS — it does not add a second encryption layer.' } },
  { id: 'd1-kms-028', domain: 1, topics: ['KMS', 'key material origin'], type: 'single', stem: 'A key with origin AWS_CLOUDHSM vs origin AWS_KMS — what is the difference?', options: [{ id: 'A', text: 'AWS_CLOUDHSM keys use RSA; AWS_KMS keys use AES-256' }, { id: 'B', text: 'AWS_KMS keys are free; AWS_CLOUDHSM keys incur a $1/month charge per key' }, { id: 'C', text: 'AWS_CLOUDHSM keys are automatically replicated to all regions; AWS_KMS keys are region-scoped' }, { id: 'D', text: 'AWS_CLOUDHSM origin means key material lives in your CloudHSM cluster, not shared KMS HSMs' }], correct: ['D'], explanation: 'Key material origin: AWS_KMS means material is generated in KMS-managed HSMs. AWS_CLOUDHSM means material is generated in and stays in your dedicated CloudHSM cluster for single-tenant isolation.', distractors: { A: 'Origin does not dictate algorithm — both origins can use AES-256 for symmetric keys.', C: 'Neither key type replicates automatically across regions — multi-region is a separate key type.', D: 'KMS charges $1/month per customer managed key regardless of origin — not free vs paid.' } },
  { id: 'd1-kms-029', domain: 1, topics: ['KMS', 'encryption context'], type: 'single', stem: 'What is KMS encryption context and what does it provide?', options: [{ id: 'A', text: 'The IAM role and session token used during encryption stored as ciphertext metadata' }, { id: 'B', text: 'A time-limited token that expires and makes ciphertext unreadable after a configurable TTL' }, { id: 'C', text: 'Non-secret key-value metadata bound to ciphertext' }, { id: 'D', text: 'A username/password pair required for all KMS API calls for additional authentication' }], correct: ['C'], explanation: 'Encryption context is an optional but powerful key-value map. It is cryptographically bound to the ciphertext. Decryption calls must provide the exact same context — different context = decryption failure. Appears in CloudTrail for audit.', distractors: { A: 'Encryption context is not authentication — IAM credentials handle authentication separately.', C: 'IAM credentials are not stored in ciphertext — encryption context is metadata you choose.', D: 'Encryption context has no TTL — it is permanent metadata bound to the ciphertext.' } },
  { id: 'd1-kms-030', domain: 1, topics: ['KMS', 'ReEncrypt'], type: 'single', stem: 'A security team wants to migrate S3 data from key A to key B without exposing plaintext. Which KMS operation achieves this?', options: [{ id: 'A', text: 'kms:ReEncrypt' }, { id: 'B', text: 'Decrypt with key A then Encrypt with key B in two separate API calls' }, { id: 'C', text: 'Delete key A' }, { id: 'D', text: 'kms:UpdateKeyDescription to associate existing ciphertext with the new key ID' }], correct: ['A'], explanation: 'kms:ReEncrypt performs decrypt+encrypt inside the KMS HSM boundary. Plaintext never leaves KMS. The caller needs Decrypt permission on the source key and Encrypt permission on the destination key.', distractors: { A: 'Two separate calls expose plaintext in memory on the calling system — less secure than ReEncrypt.', C: 'UpdateKeyDescription only changes the key description text — it does not migrate ciphertext.', D: 'KMS never auto-migrates ciphertext — deleting a key makes its ciphertext permanently inaccessible.' } },
  { id: 'd1-kms-031', domain: 1, topics: ['KMS', 'DynamoDB', 'encryption at rest'], type: 'single', stem: 'DynamoDB encryption at rest is enabled. Which KMS key options are available?', options: [{ id: 'A', text: 'Only AWS owned keys' }, { id: 'B', text: 'DynamoDB uses SSE-S3 for encryption' }, { id: 'C', text: 'Only customer managed keys' }, { id: 'D', text: 'AWS owned key (free, default), AWS managed key (DynamoDB-managed), or customer managed key (full control)' }], correct: ['D'], explanation: 'DynamoDB offers three key options: (1) AWS owned (free, default, no CloudTrail), (2) AWS managed DynamoDB key ($1/month, basic audit), (3) Customer managed key ($1/month + API costs, full control, CloudTrail per operation).', distractors: { A: 'Customer managed keys are supported and required for compliance scenarios needing full audit.', C: 'AWS owned key is the default — customer managed keys are optional.', D: 'DynamoDB uses KMS for encryption — not SSE-S3 which is an S3-specific encryption method.' } },
  { id: 'd1-kms-032', domain: 1, topics: ['KMS', 'key rotation', 'manual'], type: 'single', stem: 'Automatic KMS key rotation is enabled. How frequently does it rotate?', options: [{ id: 'A', text: 'Approximately once per year (every 365 days) for symmetric customer managed keys' }, { id: 'B', text: 'Every 30 days' }, { id: 'C', text: 'KMS rotation frequency is configurable from 90 to 2555 days per key' }, { id: 'D', text: 'Every 90 days' }], correct: ['C'], explanation: 'As of late 2023, KMS automatic rotation is configurable between 90 and 2555 days. Previously fixed at 365 days. Asymmetric, HMAC, and imported-material keys do not support automatic rotation.', distractors: { A: '90 days is the minimum, not the default — the rotation period is configurable.', B: 'Annual rotation was the original behavior but rotation frequency is now configurable.', C: '30 days is below the minimum — minimum configurable rotation period is 90 days.' } },
  { id: 'd1-kms-033', domain: 1, topics: ['KMS', 'Lambda', 'env vars'], type: 'single', stem: 'Lambda environment variables are encrypted at rest. What provides encryption above the default Lambda encryption?', options: [{ id: 'A', text: 'Lambda automatically encrypts env vars with a unique key per function per invocation' }, { id: 'B', text: 'Wrap env var values in base64' }, { id: 'C', text: 'Use a customer managed KMS key to encrypt Lambda env vars' }, { id: 'D', text: 'Store env vars in Secrets Manager and set Lambda memory to encrypted mode' }], correct: ['C'], explanation: 'By default Lambda encrypts env vars with an AWS managed key. Using a customer managed KMS key gives you key policy control, rotation control, and CloudTrail logging of every decryption at invocation.', distractors: { A: 'Lambda uses a per-function key — not per-invocation. Default is an AWS managed key, not customer-managed.', C: 'Base64 is encoding, not encryption — it provides no security.', D: 'Lambda does not have an "encrypted memory mode" — env vars and Secrets Manager are separate features.' } },
  { id: 'd1-kms-034', domain: 1, topics: ['KMS', 'cost'], type: 'single', stem: 'A customer managed KMS key is created. What determines the monthly cost?', options: [{ id: 'A', text: '$1/month per CMK plus $0.03 per 10,000 API requests (GenerateDataKey, Decrypt, etc.)' }, { id: 'B', text: 'Cost depends on the size of data encrypted and scales with storage gigabytes' }, { id: 'C', text: '$0.10/month per CMK for symmetric keys; $1/month per CMK for asymmetric keys' }, { id: 'D', text: 'KMS is free' }], correct: ['A'], explanation: 'KMS pricing: $1/month per customer managed CMK (first key for each service is often free with AWS managed keys). Cryptographic operations: $0.03 per 10,000 API calls for symmetric keys. Free tier: 20,000 requests/month.', distractors: { A: 'KMS cost is based on API calls and CMK count — not on the size of data being encrypted.', C: 'KMS has per-key and per-API pricing — it is not free for customer managed keys.', D: 'Both symmetric and asymmetric customer managed keys cost $1/month — same flat rate.' } },
  { id: 'd1-kms-035', domain: 1, topics: ['KMS', 'key condition', 'kms:CallerAccount'], type: 'single', stem: 'A KMS key policy condition should restrict usage to specific AWS accounts only. Which condition key?', options: [{ id: 'A', text: 'aws:SourceAccount to validate the account making the KMS API request' }, { id: 'B', text: 'aws:RequestedRegion to restrict key usage to accounts in specific AWS regions' }, { id: 'C', text: 'aws:PrincipalAccount to verify the calling principal\'s account ID in the request' }, { id: 'D', text: 'kms:CallerAccount to restrict key usage to specific account IDs in key policy statements' }], correct: ['D'], explanation: 'kms:CallerAccount is a KMS-specific condition key that restricts key usage to principals from specified account IDs. Use it in key policy statements to allow services from specific accounts while denying others.', distractors: { A: 'aws:PrincipalAccount is a global condition key — kms:CallerAccount is the KMS-native equivalent for key policies.', C: 'aws:SourceAccount identifies the service making a request on behalf of an account — not the direct caller account.', D: 'aws:RequestedRegion restricts which AWS regions can be targeted — not account IDs.' } },
  { id: 'd1-kms-036', domain: 1, topics: ['KMS', 'aliases'], type: 'single', stem: 'What is the operational benefit of using a KMS key alias instead of the key ID in application code?', options: [{ id: 'A', text: 'Aliases bypass key policy checks' }, { id: 'B', text: 'Aliases are free' }, { id: 'C', text: 'Aliases can be updated to point to a new key' }, { id: 'D', text: 'Aliases have lower API latency than key IDs in all cryptographic operations' }], correct: ['C'], explanation: 'Key aliases are pointers. Updating an alias to a new key ID allows transparent key replacement without updating application configuration or code. One alias per key per account per region.', distractors: { A: 'Aliases add a negligible lookup step — latency difference is not a meaningful operational benefit.', C: 'Key policies still apply when using an alias — the alias resolves to the key ID, which is then evaluated.', D: 'Both key IDs and aliases incur the same per-request KMS billing — no pricing difference.' } },
  { id: 'd1-kms-037', domain: 1, topics: ['KMS', 'CloudTrail', 'kms events'], type: 'single', stem: 'What CloudTrail event type captures KMS GenerateDataKey calls?', options: [{ id: 'A', text: 'Data events' }, { id: 'B', text: 'Management events' }, { id: 'C', text: 'Network activity events' }, { id: 'D', text: 'Insight events' }], correct: ['B'], explanation: 'KMS API calls (Encrypt, Decrypt, GenerateDataKey, etc.) are management events — logged by default in all CloudTrail trails at no extra cost. Data events are for S3 object-level and Lambda invocations.', distractors: { A: 'KMS cryptographic operations are management events — not data events like S3 PutObject.', C: 'Network activity events are a separate CloudTrail event type for VPC endpoint activity — not KMS.', D: 'Insight events detect anomalous API call rates — they do not replace management event logging.' } },
  { id: 'd1-kms-038', domain: 1, topics: ['KMS', 'parameter store', 'SecureString'], type: 'single', stem: 'A SecureString parameter in SSM Parameter Store uses KMS. What permission is required to retrieve it?', options: [{ id: 'A', text: 'kms:Decrypt only' }, { id: 'B', text: 'ssm:GetParameter only' }, { id: 'C', text: 'ssm:GetParameter plus kms:Decrypt on the KMS key used to encrypt the parameter' }, { id: 'D', text: 'ssm:GetSecureString' }], correct: ['C'], explanation: 'Retrieving a SecureString requires both ssm:GetParameter (SSM service permission) and kms:Decrypt (to decrypt the value). Both must be granted — SSM does not auto-grant KMS access.', distractors: { A: 'SSM does not transparently handle decryption — the caller needs explicit kms:Decrypt permission.', C: 'There is no ssm:GetSecureString API — GetParameter retrieves all parameter types including SecureString.', D: 'KMS permissions alone are insufficient — SSM service permissions are also required.' } },
  { id: 'd1-kms-039', domain: 1, topics: ['KMS', 'key policy', 'least privilege'], type: 'single', stem: 'Which key policy statement follows least privilege for an S3 service using SSE-KMS?', options: [{ id: 'A', text: 'Allow kms:* to the account root only and rely on S3 to inherit access automatically' }, { id: 'B', text: 'Allow kms:ReEncrypt to Principal:* since S3 re-encrypts all objects periodically' }, { id: 'C', text: 'Allow kms:* to Principal:* so S3 can use the key for all operations' }, { id: 'D', text: 'Allow kms:GenerateDataKey and kms:Decrypt to the S3 service principal only' }], correct: ['D'], explanation: 'S3 needs only kms:GenerateDataKey (for new encryptions) and kms:Decrypt (for reading). Limiting to these two actions and the s3.amazonaws.com service principal follows least privilege.', distractors: { A: 'kms:* grants all KMS actions including key deletion — far more than S3 needs.', C: 'Allowing kms:* to root is overly broad and does not explicitly grant S3 service principal access.', D: 'S3 does not periodically re-encrypt objects — kms:ReEncrypt is not needed by S3.' } },
  { id: 'd1-kms-040', domain: 1, topics: ['KMS', 'EBS', 'snapshot sharing'], type: 'single', stem: 'An encrypted EBS snapshot must be shared with another AWS account. What is required?', options: [{ id: 'A', text: 'Encrypted snapshots cannot be shared' }, { id: 'B', text: 'Share the snapshot only' }, { id: 'C', text: 'Share the snapshot AND grant the target account permission on the KMS key used to encrypt it' }, { id: 'D', text: 'Copy the snapshot to the target account using STS AssumeRole before sharing' }], correct: ['C'], explanation: 'Sharing encrypted snapshots: modify snapshot permissions to include the target account, AND update the KMS key policy to allow the target account to use the key. Without key access, the target account cannot restore the volume.', distractors: { A: 'Encrypted snapshots can be shared — but KMS key access must also be granted.', C: 'The target account cannot use their own key to decrypt a snapshot encrypted with a different key automatically.', D: 'STS AssumeRole is for API access — not for cross-account snapshot sharing workflows.' } },
  { id: 'd1-kms-041', domain: 1, topics: ['KMS', 'custom key store'], type: 'single', stem: 'A company wants KMS-integrated encryption but with key material stored in their dedicated CloudHSM cluster. What KMS feature supports this?', options: [{ id: 'A', text: 'KMS custom key store backed by CloudHSM' }, { id: 'B', text: 'AWS Encryption SDK configured to call CloudHSM directly, bypassing KMS entirely' }, { id: 'C', text: 'KMS key import with AWS_CLOUDHSM origin' }, { id: 'D', text: 'KMS multi-region keys with CloudHSM as the secondary replication target' }], correct: ['A'], explanation: 'KMS custom key stores use CloudHSM clusters as the backing store. All KMS APIs work normally, but key material never leaves the customer\'s dedicated CloudHSM hardware — satisfying compliance requirements for sole control.', distractors: { A: 'AWS_CLOUDHSM origin is used for custom key stores — but the material goes to YOUR CloudHSM, not shared KMS HSMs.', C: 'Multi-region keys are about geographic replication — not about using CloudHSM as the backing store.', D: 'The Encryption SDK can call CloudHSM directly but then you lose KMS service integration (S3, RDS, etc.).' } },
  { id: 'd1-kms-042', domain: 1, topics: ['KMS', 'key spec', 'asymmetric signing'], type: 'single', stem: 'A signing service needs to create digital signatures verifiable by external parties without AWS credentials. Which KMS key spec?', options: [{ id: 'A', text: 'SYMMETRIC_DEFAULT' }, { id: 'B', text: 'KEY_AGREEMENT' }, { id: 'C', text: 'HMAC_256' }, { id: 'D', text: 'RSA_2048 or ECC_NIST_P256 asymmetric key spec with SIGN_VERIFY usage' }], correct: ['D'], explanation: 'Asymmetric signing: KMS signs with private key (Sign API) inside AWS. External parties download the public key and verify the signature without AWS credentials or KMS access.', distractors: { A: 'Symmetric keys cannot be used for digital signatures — asymmetric key pairs are required.', C: 'HMAC verification requires the secret key — external parties cannot verify HMACs without the shared secret.', D: 'KEY_AGREEMENT keys are for key exchange (Diffie-Hellman) — not for digital signing operations.' } },
  { id: 'd1-kms-043', domain: 1, topics: ['KMS', 'audit', 'compliance'], type: 'single', stem: 'Compliance requires proof that all KMS key usage was logged and no key operations occurred outside AWS. What provides this?', options: [{ id: 'A', text: 'CloudTrail management events capturing all KMS API calls with caller identity and timestamp' }, { id: 'B', text: 'KMS automatic compliance report available in the KMS console under Compliance tab' }, { id: 'C', text: 'AWS Config KMS rule recording all encryption events and policy changes' }, { id: 'D', text: 'KMS key usage report in the console showing monthly operation counts per key' }], correct: ['A'], explanation: 'CloudTrail records every KMS API call — Encrypt, Decrypt, GenerateDataKey, CreateKey, etc. — with request time, calling principal, key ID, and response. This is the authoritative audit trail for KMS usage compliance.', distractors: { A: 'The KMS console shows basic metrics — CloudTrail provides the detailed immutable audit records compliance requires.', C: 'AWS Config tracks resource configuration changes (key policy updates, key enable/disable) — not cryptographic operation logs.', D: 'There is no automatic compliance report in the KMS console — CloudTrail is the audit source.' } },
  { id: 'd1-kms-044', domain: 1, topics: ['KMS', 'delete key'], type: 'single', stem: 'What happens to all ciphertext encrypted by a KMS key when the key is permanently deleted?', options: [{ id: 'A', text: 'Ciphertext is archived for 7 years per AWS data retention policy before deletion' }, { id: 'B', text: 'All ciphertext becomes permanently undecryptable' }, { id: 'C', text: 'AWS automatically re-encrypts all data with the account default key before deletion completes' }, { id: 'D', text: 'AWS Support can recover key material within 30 days of deletion for emergency access' }], correct: ['B'], explanation: 'Deleting a KMS key is irreversible. All ciphertext encrypted by the key becomes permanently inaccessible. This is why the 7–30 day waiting period exists — to allow cancellation if services depend on the key.', distractors: { A: 'AWS does not auto-re-encrypt data on key deletion — this is the customer\'s responsibility before scheduling deletion.', C: 'AWS does not retain a ciphertext archive — deleted ciphertext becomes unrecoverable.', D: 'AWS Support cannot recover deleted KMS key material — once deleted, data is unrecoverable.' } },
  { id: 'd1-kms-045', domain: 1, topics: ['KMS', 'key arn', 'account migration'], type: 'single', stem: 'An application hardcodes a KMS key ARN. The key is deleted and a new one created. What must change?', options: [{ id: 'A', text: 'Re-enable the deleted key from the KMS console' }, { id: 'B', text: 'Update the KMS key alias to point to the new key ARN' }, { id: 'C', text: 'Update the application code or configuration to reference the new key ARN' }, { id: 'D', text: 'The new key automatically inherits the old key ARN when created as a replacement' }], correct: ['C'], explanation: 'Key ARNs are immutable identifiers unique to each key. A new key always gets a new ARN. Hardcoded ARNs must be updated. This is why using aliases is recommended — aliases can be repointed without code changes.', distractors: { A: 'Aliases must be explicitly created and associated with a key — updating an alias requires the application to reference an alias, not an ARN.', C: 'New KMS keys always receive new unique ARNs — they do not inherit previous key identifiers.', D: 'KMS key deletion is permanent after the waiting period — deleted keys cannot be re-enabled.' } },
]
